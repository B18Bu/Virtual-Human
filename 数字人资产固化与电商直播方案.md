# 数字人资产固化与电商直播数字人方案

> **目标读者**：已经用 Linly-Talker 试出满意效果、准备把某个「形象 + 音色」定下来并往电商直播方向推进的操盘者。
>
> **本文档回答**：怎么把确认好的数字人**存下来**、权重怎么**保**、要**训什么**、离电商直播还差**哪几步**。
>
> 核验日期：2026-09-16。所有「不能 / 缺失 / 未接线」的结论都带 `文件:行号` 证据。
>
> ✅ **本文档配套的 `dh.py` 已实机跑通全流程**（建档案 → 校验 → 出片 → 打包），实测数据见 §2.6。
>
> **相关文档**：`Linly-Talker使用手册.md`（日常操作）、`音色克隆训练方案.md`（音色原理）、`部署进度与交接.md`（部署踩坑）。
> 本文档不重复它们的内容，只讲**资产化**与**电商化**这两件新事。

---

## 0. 先纠正一个前提（很重要，否则整份方案会跑偏）

你的提问是「保存形象和音频 → 训练成电商直播数字人」。这个描述里藏着一个**与系统实际不符**的预设：**以为形象是要训练的。**

实际上，这个系统里「数字人」不是一个模型，而是**三层资产的组合**：

| 层 | 是什么 | 怎么来 | 要不要训练 |
|---|---|---|---|
| **形象层** | 一张图（SadTalker/Wav2Lip）或一段视频（MuseTalk） | 拍/选/上传 | ❌ **零样本，不训练** |
| **声音层** | 音色权重（GPT-SoVITS 微调）或一段参考音频 | 录音 + 训练/即用 | ✅ **要训练**（已实现） |
| **驱动层** | 引擎选择 + 参数 + 人设话术 | 配置 | ❌ 是配置，不是训练 |

**三句话结论**：

1. **形象不需要训练**。SadTalker 拿到一张图就能出片，`inputs/girl.png` 这种 137KB 的图就够了。你「确认好形象」这个动作，本质上就是**选定一张图**。
2. **要训练的只有音色**，而且**已经实现并跑通了**（`/root/autodl-tmp/voice-train/pipeline.py` + `POST /api/v1/voices`）。
3. **「保存数字人」这个功能在当前系统里完全不存在**——这是本文档要补的第一块，也是你最该先做的一块。

至于「电商直播」，真正卡住你的**不是训练**，而是三个**没接线**的东西（LLM、商品知识、直播编排）。见 §5。

---

## 1. 现状清查：为什么现在存不下来

### 1.1 形象：零持久化

| 链路 | 产物落点 | 复用 |
|---|---|---|
| SadTalker | `results/<uuid4>/`，**每次生成新建**（`TFG/SadTalker.py:200-207`） | ❌ 无 |
| Wav2Lip | 写死 `results/example_answer.mp4` 后被搬走（`adapter.py:416-417`） | ❌ 无 |
| MuseTalk | `results/avatars/<视频文件名>/`（`TFG/MuseTalk.py:190-201`） | ⚠️ 半成品 |

你上传的图会落在 `linly-api/uploads/upload-<task_id>-<毫秒>.png`（`storage.py:56`），**任务成功 6 小时后被连记录一起删掉**（`queue.py:179-180` 会同时删结果文件和上传图片，TTL = `TASK_TTL_SECONDS` = 21600 秒）。

> ⚠️ **这意味着：你现在「确认好的形象」，如果没另存一份，6 小时后自动消失。**

### 1.2 唯一接近「保存数字人」的机制，是个被阉割的半成品

MuseTalk 有一条 `results/avatars/{avatar_id}/` 的资产目录（`TFG/MuseTalk.py:190-201`），但：

| 缺陷 | 位置 |
|---|---|
| 元数据字段定义被**注释掉**（原设计 `avatar_id`/`video_path`/`bbox_shift`） | `TFG/MuseTalk.py:131-135` |
| 写 `avator_info.json` 的 `json.dump` 也被**注释掉** → 元数据**根本不落盘** | `TFG/MuseTalk.py:264-267` |
| 目录已存在就 `shutil.rmtree` 全删重建，**没有「已存在则复用」分支** | `TFG/MuseTalk.py:202-208` |
| `avatar_id` = 视频文件名去后缀，**与文件路径强耦合**，无独立命名空间 | `TFG/MuseTalk.py:193` |

对比**官方实现是完整的**：`Musetalk/scripts/realtime_inference.py:64-70` 定义 `avatar_info`、`:137-138` 落盘、`:104-111` 读回并比对 `bbox_shift`。**Linly-Talker 的封装把这个复用逻辑删掉了。**

实测：`results/avatars/` **当前是空目录**。

### 1.3 音色：唯一被真正资产化了的东西

`/root/autodl-tmp/voices/<voice_id>/` —— 这是全系统**唯一**有完整生命周期管理的资产（可枚举、可查询、可删除）。

但它有一个**致命的自包含缺口**，见 §3.2。

### 1.4 服务层没有任何「档案」概念

对 `linly-api/app/*.py` 全文检索 `asset|profile|preset|avatar|persona|数字人档案|资产`：
**没有任何类、表、JSON 或端点**把「图片 + 音色 + 引擎配置」组合成一个可复用档案。唯一的 `avatar` 命中全是 MuseTalk 的临时视频变量名（`adapter.py:448/451/453/457/693/697-699`）。

---

## 2. 角度一：形象与音频的保存

### 2.1 设计：数字人档案（Digital Human Profile）

**核心思路**：不改 Linly-Talker 一行代码，在仓库外建一个**单一真相来源**的档案目录。所有「确认好的数字人」都长这样：

```
/root/autodl-tmp/digital-humans/<profile_id>/
├── profile.json      # 档案主文件（唯一真相来源）
├── avatar.png        # 定版形象图（原图，勿压缩）
├── ref.wav           # 3~10 秒参考音频（音色的 prompt）
├── ref.txt           # 参考音频的逐字文本
├── products.json     # 商品库（电商场景，可选）
└── preview.mp4       # 定版试片（验收凭证）
```

选 `/root/autodl-tmp/digital-humans/` 的理由：
- **在仓库外** → `git status` 保持干净（与 `linly-api/`、`voice-train/` 的既定原则一致）
- **不在 `uploads/`** → 不受 6 小时 TTL 清理
- **路径不含 `pretrained` 子串** → 避开 `VITS/GPT_SoVITS.py:351` 的子串匹配陷阱（这条坑对音色权重是致命的，见 §3.2）

### 2.2 `profile.json` 结构

```json
{
  "profile_id": "xiaomei",
  "display_name": "小美 · 美妆主播",
  "version": 1,
  "created_at": "2026-09-16T21:30:00+08:00",
  "updated_at": "2026-09-16T21:30:00+08:00",

  "avatar": {
    "image": "avatar.png",
    "sha256": "<64 位十六进制>",
    "width": 640,
    "height": 1024,
    "engine": "sadtalker",
    "preprocess": "full",
    "still_mode": false,
    "enhancer": false
  },

  "voice": {
    "engine": "finetuned",
    "voice_id": "dh_smoke",
    "ref_audio": "ref.wav",
    "ref_text": "书架上摆满了各种各样的书籍，从历史到科学应有尽有。",
    "lang": "中文",
    "weights": {
      "gpt": "/root/autodl-tmp/voices/dh_smoke/gpt.ckpt",
      "sovits": "/root/autodl-tmp/voices/dh_smoke/sovits.pth"
    }
  },

  "persona": {
    "system_prompt": "你是美妆主播小美，语气亲切专业，回答控制在 60 字以内。",
    "products_file": "products.json",
    "disclosure": "本直播间由 AI 数字人主播"
  },

  "render": { "fps": 20, "size": 256 }
}
```

**关键设计点**：

| 字段 | 为什么这么设计 |
|---|---|
| `avatar.sha256` | 图被换过能立刻发现。形象是「定版」资产，**内容必须可校验** |
| `avatar.preprocess` **必须 `"full"`** | `crop` 只出 256×256 纯人脸、约 63KB；`full` 出整幅人像、260~370KB。两套配套 mapping 也不同（`src/utils/init_path.py:19,22`） |
| `voice.ref_audio` + `ref_text` **落进档案** | 补掉 §3.2 的自包含缺口——调用方不必再手找参考音频 |
| `voice.weights` 存**绝对路径** | 音色权重不复制进档案，只引用。避免 85MB × N 份的冗余 |
| `persona` 与形象**绑定在同一个档案** | 现状是二者完全无关（见 §5.2），绑起来才能叫「一个数字人」 |

### 2.3 实施：`dh.py` 档案管理脚本

**已实现并跑通**，位置：

```
/root/autodl-tmp/digital-humans/dh.py
```

> 本文档不内联完整源码（约 400 行，见该文件），避免文档与代码漂移。
> 下面只讲**两处非显然的设计**，其余是常规的 argparse 封装。

**设计点 1：参考音频自动挑选**（补掉音色不自包含缺口）

训练产物 `meta.json` 里只有 `source_audio`（原始长音频）和 `work_dir`，**没有可直接当 prompt 的 3~10 秒片段**。但训练语料其实**就在旁边**：`linly-api/voice_work/<id>/slicer_opt/*.wav`（切片）+ `transcript.txt`（文本）。

```python
def pick_reference(voice_id: str):
    """从训练语料里挑一段 3~10 秒的切片 + 它的文本。"""
    work = VOICE_WORK_ROOT / voice_id
    slices_dir, tr = work / "slicer_opt", work / "transcript.txt"
    if not slices_dir.is_dir() or not tr.is_file():
        return None

    texts = {}
    for line in tr.read_text(encoding="utf-8").splitlines():
        if not line.strip() or line.startswith("#"):
            continue                       # transcript.txt 头两行是 # 注释
        name, _, txt = line.partition("\t")  # 左列是完整文件名（含 .wav）
        if name and txt.strip():
            texts[Path(name).stem] = txt.strip()

    best = None
    for w in sorted(slices_dir.glob("*.wav")):
        txt = texts.get(w.stem)
        if not txt:
            continue
        dur = wav_duration(w)
        # GPT-SoVITS 硬约束：短于 3 秒或长于 10 秒会 raise OSError
        if 3.0 <= dur <= 10.0:
            score = abs(dur - 5.5)          # 偏好 4~7 秒
            if best is None or score < best[0]:
                best = (score, w, txt)
    return (best[1], best[2]) if best else None
```

> 📌 **易错点**：切片文件名带采样点后缀（如 `..._0001416640_0001593600.wav`），
> 而 `transcript.txt` 左列是**完整文件名**。两侧都用 `Path(...).stem` 才能对上。

**设计点 2：先全部校验，再落盘**（原子性）

第一版把 `mkdir` 放在最前面，结果「音色权重缺失」这种校验失败会**留下一个空的档案目录**——实测踩到了。正确顺序是：

```
阶段一（不落盘）：校验形象图存在+后缀+PIL可读 → 校验音色权重 → 解析参考音频
阶段二（落盘）  ：mkdir → 复制图 → 复制 ref.wav → 写 ref.txt → 写 profile.json
```

### 2.4 使用

```bash
cd /root/autodl-tmp/digital-humans

# ① 从「确认好的形象图」+「训好的音色」建档案
python dh.py create --id xiaomei --name "小美·美妆主播" \
  --image /root/autodl-tmp/Linly-Talker/inputs/girl.png \
  --voice-id dh_smoke --engine sadtalker \
  --system-prompt "你是美妆主播小美，语气亲切专业，回答控制在60字内。"

# ② 校验（形象 sha256 + 音色权重 + 参考音频三件套）
python dh.py verify xiaomei

# ③ 出试片
python dh.py render xiaomei --text "欢迎来到我的直播间"

# ④ 列出 / 查看
python dh.py list
python dh.py show xiaomei

# ⑤ 迁移到另一台机器
python dh.py export xiaomei --with-weights --out /tmp/xiaomei.tar.gz
```

**没训音色也能先用**：

```bash
python dh.py create --id demo --image face.png --voice-engine edge --speaker zh-CN-XiaoxiaoNeural
```

先把形象定下来，音色 later 再换。

### 2.5 形象图的技术要求（决定成片质量上限）

| 项 | 要求 | 原因 |
|---|---|---|
| 构图 | **留够头肩**，别贴脸裁 | 成片是整幅人像，源图裁得越紧可动范围越小 |
| 分辨率 | ≥ 512×512，推荐 640×1024 竖版 | 直播是竖屏场景 |
| 人脸 | 正脸、五官清晰、无遮挡 | SadTalker 要抽 3DMM 系数，侧脸/遮挡会崩 |
| 格式 | `.png/.jpg/.jpeg/.bmp/.webp` | API 白名单（`config.py:44`） |
| 大小 | ≤ 10 MB | `MAX_IMAGE_BYTES`（`config.py:39`） |
| **`preprocess`** | **必须 `full`** | 见下 |

> 🔴 **`preprocess` 是形象资产的一部分，不是画质调优项。**
> `crop` → 256×256 纯人脸、约 63KB；`full` → 整幅人像、260~370KB。两套配套 mapping 也不同（`full` 用 `mapping_00109` + `facerender_still.yaml`，`crop` 用 `mapping_00229` + `facerender.yaml`，`src/utils/init_path.py:19,22`），**别只换一个**。
> 6006 API 路径已固定走 `full`（`adapter.py:361-386`），但 Gradio 路径默认仍是 `crop`（`webui.py:517,622`）——**用 Gradio 时必须手动改**。

### 2.6 实测结果（2026-09-16）

全部子命令实机跑通：

| 操作 | 结果 |
|---|---|
| `create`（edge 音色） | ✅ 640×1024 形象，sha256 记录 |
| `create`（finetuned 音色） | ✅ **自动挑中 5.5 秒切片** + 配对文本，正是偏好窗口中心 |
| `verify` | ✅ 形象 sha256 / 音色权重 / 参考音频三项全绿 |
| `render`（edge） | ✅ **18.0 秒**，420 KB，640×1024 |
| `render`（finetuned） | ✅ **17.4 秒**，442 KB |
| `export` / `list` / `show` | ✅ |
| 失败路径（缺权重 / 图不存在 / 非法档案名） | ✅ 均正确报错且**不留空壳目录** |

**产物技术规格**（`ffprobe` 实读）：

```
h264  640x1024  4.6s  92 帧   +   aac 16kHz 单声道
PCM 峰值 24425 / RMS 3220   →  有声音（非静音）
```

> 📌 **验收线：产物 260~370 KB / 640×1024**。
> 实测 420~442 KB 属正常（文本更长）。**低于 200 KB 就要警惕**——那是踩了 `crop` 坑。
> 📌 **一定要量 PCM**：EdgeTTS 的 VOLUME 参数是反的（`TTS/EdgeTTS.py:146` 做 `volume = 100 - volume`），传 0 会得到**格式合法、长度正常、内容全零**的静音音轨——`ffprobe` 查不出来。

---

## 3. 角度二：权重保存

### 3.1 权重分层：哪些该跟数字人走，哪些不该

**先建立分层观念**——不是所有 `.pth` 都是「你的」。

| 层 | 权重 | 体量 | 跟数字人走？ | 说明 |
|---|---|---|---|---|
| **L1 底座** | SadTalker / Wav2Lip / MuseTalk / CosyVoice / GPT-SoVITS 底座 | **约 17.7 GB** | ❌ **绝不** | 全网公共，重下即可。备份它纯属浪费 |
| **L2 微调** | `voices/<id>/gpt.ckpt` + `sovits.pth` | **约 240 MB/音色**（实测 148+85 MB） | ✅ **必须** | 训练产物，**丢了就要重跑且有随机性** |
| **L3 形象** | ER-NeRF 的 `.pth` + `.json` | **约 12 MB/人** | ✅ 若走 NeRF 路线 | 训练产物，见 §4.4 |
| **L4 素材** | 形象图 / 参考音频 / 训练语料 | KB~MB 级 | ✅ **必须** | 前两样在档案里；**语料常被忽略，但它决定能否复现** |

> ⚠️ **训练语料是易被忽略的关键资产。** 它躺在 `linly-api/voice_work/<id>/`（实测包含 `slicer_opt/` 16 个切片、`transcript.txt`、`dataset/`、`logs_s1/`、训练配置等），而 `TRAIN_TTL_SECONDS`（`config.py:66`）**定义后从未被使用**——目前不会自动清理，但**也没有任何东西保证它不被误删**。想复现/迭代音色就必须留着。

### 3.2 音色权重（已实现）——以及一个必须补的缺口

**位置**：`/root/autodl-tmp/voices/<voice_id>/`（实测）

```
gpt.ckpt     155,085,712 B (148 MB)    s1（GPT，学「文本→语义 token」）
sovits.pth    84,930,712 B ( 81 MB)    s2（SoVITS，音色主要来自这里）
meta.json            465 B
```

**`meta.json` 的 9 个字段**（实测，`voice-train/pipeline.py:486` + `:550-553`）：

```json
{
  "segments": 16,
  "epochs_s1": 4,
  "epochs_s2": 4,
  "source_audio": "/root/autodl-tmp/linly-api/uploads/voice-dh_smoke-1789564358578.wav",
  "work_dir": "/root/autodl-tmp/linly-api/voice_work/dh_smoke",
  "voice": "dh_smoke",
  "s1_source": ".../voice_work/dh_smoke/gpt_weights/dh_smoke-e4.ckpt",
  "s2_source": ".../voice_work/dh_smoke/sovits_weights/dh_smoke_e4_s48.pth",
  "created_at": "2026-09-16 21:14:05"
}
```

**加载方**：`adapter._finetuned_paths`（`adapter.py:536-559`）→ `_gpt_sovits(voice_id)`（`adapter.py:561-587`）→ `GPT_SoVITS().load_model(gpt, sovits)`

> 📌 **判断加载成功的标志**：日志里出现 `<All keys matched successfully>`。
> 📌 **`sovits.pth` 比底座小是正常的**（实测 81 MB vs 底座 101 MB）——小的那部分是 `enc_q` 被正确剥离了。

**🔴 三条硬约束：**

1. **路径绝不能含 `"pretrained"` 子串。**
   `VITS/GPT_SoVITS.py:351` 做的是子串匹配：`if ("pretrained" not in sovits_path): del vq_model.enc_q`。微调产物本就不含 `enc_q`，路径里一旦出现 `pretrained` 就走错分支。
   ⇒ 所以权重放 `/root/autodl-tmp/voices/`，**绝不放** `GPT_SoVITS/pretrained_models/`。这条已在 `config.py:60-62` 写成硬注释。

2. **文件名固定** `gpt.ckpt` / `sovits.pth`，**不要带 epoch 号**。适配层按目录名 + 固定文件名解析（`adapter.py:553`）。

3. **每个音色常驻约 240 MB 显存，且缓存永不淘汰**（`adapter.py:561-587`，无 LRU 无上限）。**数字人档案多了会爆显存**——这是 §7 要处理的运维点。

**🔴 最该补的缺口：音色目前「不自包含」**

`finetuned:` 语法**强制调用方每次自带参考音频 + 文本**（`adapter.py:337-340`）：

```
finetuned:<音色名>|<参考音频>|<参考文本>[|<语言>]
```

但 `meta.json` 里只有 `source_audio`（原始长音频，几十分钟）和 `work_dir`，**没有一段 3~10 秒的参考片段及其文本**。也就是说：**训练好的音色自己描述不了自己**，用户每次都得翻回原始录音另切一段。服务端返回的 `usage` 字段也是这个现象的体现：

```
"usage": "finetuned:dh_smoke|<参考音频>|<参考文本>|中文"     ← 占位符要用户手填
```

**§2.3 的 `pick_reference()` 就是补这个的**：建档案时自动从 `voice_work/<id>/slicer_opt/*.wav` 挑一段 3~10 秒切片 + 配对 `transcript.txt` 文本，落成档案里的 `ref.wav` + `ref.txt`。建完之后**音色就自包含了**：

```
finetuned:dh_smoke|/root/autodl-tmp/digital-humans/xiaomei/ref.wav|书架上摆满了各种各样的书籍，从历史到科学应有尽有。|中文
```

> 💡 **更彻底的做法**（可选，需改 `voice-train/pipeline.py`）：在 `register_voice`（`pipeline.py:470-489`）里就把参考片段登记进 `meta.json`，让音色在**训练完成那一刻**就自包含。与 `pick_reference` 不冲突，可叠加。

### 3.3 形象权重（仅 ER-NeRF 路线）

| 文件 | 大小 | 状态 |
|---|---|---|
| `checkpoints/Obama_ave.pth` | 8.0 MB | ✅ **唯一被代码加载的**（`webui.py:873`、`app_talk.py:210`、`api/talker_api.py:73`） |
| `checkpoints/Obama.json` | 4.4 MB | ✅ NeRF 相机位姿轨迹（`provider.py:104-105`） |
| `checkpoints/Obama.pth` | 7.5 MB | ⚠️ **零代码引用** |
| `checkpoints/May.pth` | 7.5 MB | ⚠️ **零代码引用** |
| `checkpoints/May.json` | 3.3 MB | ⚠️ **零代码引用** |

> 📌 `May.*` 和 `Obama.pth` 全仓库 grep 只在 README 文件树里出现（`README.md:955-959`），**没有任何代码加载它们**——是随发行版附带的孤儿文件。真正跑起来的是 `Obama_ave.pth` + `Obama.json`。
>
> 📌 判断一个 `.pth` 是不是「数字人形象权重」的**体量特征**：**7~8 MB**。音色权重是 81~148 MB，别搞混。
>
> 📌 NeRF 形象是**单人专用**的——`webui.py:874` 明确警告「NeRFTalk 模型仅针对单个人训练，内置奥巴马模型，**上传其他图片无效**」。

### 3.4 备份与迁移

**音色（最该备份的）**：

```bash
# 打包一个音色的完整资产（权重 + 元数据 + 训练语料）
ID=dh_smoke
mkdir -p /root/autodl-tmp/backup
tar czf /root/autodl-tmp/backup/voice-$ID-$(date +%Y%m%d).tar.gz \
  -C /root/autodl-tmp voices/$ID \
  -C /root/autodl-tmp/linly-api/voice_work $ID

# 跨实例迁移后恢复
tar xzf voice-$ID-*.tar.gz -C /
```

**数字人档案**：用 `dh.py export <id> --with-weights`。

**跨 AutoDL 实例迁移的完整清单**：

| 资产 | 源路径 | 恢复方式 |
|---|---|---|
| 数字人档案 | `/root/autodl-tmp/digital-humans/` | 直接拷目录 |
| 音色权重 | `/root/autodl-tmp/voices/<id>/` | 直接拷目录 |
| 训练语料 | `linly-api/voice_work/<id>/` | 直接拷目录 |
| 服务代码 | `/root/autodl-tmp/linly-api/` | 直接拷目录（与仓库解耦，就是为了这个） |
| 训练流水线 | `/root/autodl-tmp/voice-train/` | 直接拷目录 |
| 项目代码 + 17.7G 权重 | `/root/autodl-tmp/Linly-Talker/` | **重新克隆 + 重下**，别拷 |

> ⚠️ **磁盘现状**：50G 已用 27G，**可用 24G**。L2+L3+L4 全加起来通常不到 1G，**备份成本很低，却是唯一不可再生的部分**。L1 底座 17.7G 反而是最不值得备份的。

### 3.5 版本管理

档案里的 `version` 字段（§2.2）就是干这个的。建议约定：

- **形象换图** → `version + 1`，`avatar.sha256` 变化，`preview.mp4` 重出
- **音色重训** → 换一个新的 `voice_id`（如 `dh_smoke_v2`），**不要原地覆盖**
  > 理由：`_gpt_sovits` 的缓存按 `voice_id` 分档且**永不淘汰**（`adapter.py:567`）。原地覆盖同名文件，已加载的旧实例**不会重载**，会出现「文件换了但服务还在用旧的」这种极难排查的现象。**换名 = 强制重载**，这是零成本的规避方式。
- **旧版不删** → 保留上一版至少 7 天，便于 A/B 与回滚

---

## 4. 角度三：数字人训练

### 4.1 三条路线总览

| 路线 | 训什么 | 现状 | 需要什么 | 实测耗时 |
|---|---|---|---|---|
| **A. 形象零训练** | 不训 | ✅ **今天就能用** | 一张图 | 0 |
| **B. 音色微调** | GPT-SoVITS s1+s2 | ✅ **已实现并跑通** | 1~30 分钟干净人声 | **88.1 秒**（16 段/80 秒语料/4+4 轮） |
| **C. ER-NeRF 形象训练** | NeRF 辐射场 | ⚠️ **代码在，缺入口和依赖** | **5 分钟视频 + 数小时预处理** | **数天工程** |

**建议顺序：A → B →（有明确收益再考虑）C。**

对电商直播来说，**B 的投入产出比远高于 C**——观众能立刻听出音色像不像，但很少有人能看出 NeRF 相比 SadTalker 的头部细节提升。详见 §4.4.5。

### 4.2 路线 A：形象零训练（直接可用）

不用做任何事。把图放进档案即可（§2.3）。三个引擎的差异：

| 引擎 | 输入 | 出片 | 备注 |
|---|---|---|---|
| **`sadtalker`** | 单图 | 整幅人像，带头部动作 | **推荐**。API 已固定 `full` |
| `wav2lip` | 单图 | 仅嘴部替换 | 最省显存 |
| `musetalk` | **视频** | 半身 | 张口度约为 SadTalker 一半（模型特性） |

### 4.3 路线 B：音色微调（已实现）

**已跑通，实测 88.1 秒**（16 段 / 80 秒语料 / 4+4 轮 / auto_train）。完整步骤见 `Linly-Talker使用手册.md` §7.3，这里只列**踩过的坑**：

| # | 坑 | 后果 |
|---|---|---|
| 1 | **路径含 `"pretrained"`** | `GPT_SoVITS.py:351` 走错分支 |
| 2 | **ASR 错字不校对** | 实测把「**待办**事项」认成「**代办**事项」，直接教坏模型 |
| 3 | **训练 list 用 `zh`，推理用「中文」** | 两处语言码不一致，混用直接报错 |
| 4 | **日志追加写，监视器要从偏移读** | 否则重读上一轮把状态改回去（`voices.py:314-317`） |
| 5 | **训练不走 `TaskQueue`** | 是有意的：几十分钟的长任务塞进单 worker 会把口播请求全堵死（`voices.py:1-7` 注释） |
| 6 | **训练与推理抢显存** | 训练吃满 24G，跑训练前先停 6006 服务 |

**语料要求**（决定效果上限，比训练参数重要得多）：

| 项 | 要求 |
|---|---|
| 采样率 | **≥ 32 kHz** |
| 时长 | 5~30 分钟最佳（1 分钟就能出效果） |
| 内容 | 单人朗读、语气平稳、覆盖目标场景会用到的发音（**多音字、数字、中英混读**） |
| 避免 | 笑声、口头语（嗯/啊）、背景音乐、回声、语速剧变 |

> 💡 **电商场景的语料建议**：专门录一段**商品名、价格、规格、促销话术**。这些是直播间高频词，通用语料里覆盖不到，而它们读错最致命。

### 4.4 路线 C：ER-NeRF 形象训练（重投入，谨慎）

#### 4.4.1 好消息：训练代码**是完整的**

**纠正一个常见误判**：「ER-NeRF 训练端被摘掉了」是**错的**。核实结果是：**训练能力以库的形式完整存在**，只是没有 CLI 入口。

| 组件 | 位置 | 状态 |
|---|---|---|
| `class Trainer` | `NeRF/nerf_triplane/utils.py:568` | ✅ |
| `train_step()` | `NeRF/nerf_triplane/utils.py:718` | ✅ |
| `train()` | `NeRF/nerf_triplane/utils.py:967` | ✅ |
| `train_gui()` / `train_one_epoch()` | `utils.py:1057` / `:1217` | ✅ |
| `save_checkpoint()` / `load_checkpoint()` | `utils.py:1403` / `:1465` | ✅ |
| `class NeRFGUI`（带训练循环） | `NeRF/nerf_triplane/gui.py:72`，`train_step()` 在 `:140` | ✅ |
| `class NeRFDataset`（**训练集**） | `NeRF/nerf_triplane/provider.py:387` | ✅ |
| **数据预处理全流程** | `NeRF/data_utils/process.py`（15.9 KB，9 个 task） | ✅ |
| 3DMM 人脸追踪 | `NeRF/data_utils/face_tracking/`（`face_tracker.py` 14 KB + `convert_BFM.py`） | ✅ |
| 人脸解析 | `NeRF/data_utils/face_parsing/` | ✅ |

`NeRF/nerf_triplane/gui.py:534` 的 viewport 标题是 `'RAD-NeRF'`——这是 ER-NeRF / RAD-NeRF 的 triplane backbone。

#### 4.4.2 坏消息：缺 6 样东西

| # | 缺什么 | 证据 | 补法 |
|---|---|---|---|
| 1 | **没有 CLI 入口 `main.py`** | `gui.py` 末尾只有 `render()`，无 `__main__`、无 `argparse` | 自己写（上游 `main.py` 是薄封装，约 100 行） |
| 2 | **`pytorch3d` 未安装** | `face_tracking/render_3dmm.py:5-6,22` 需要 | `pip install pytorch3d`（编译耗时，**先装 ninja**） |
| 3 | **CUDA 扩展未编译** | `find NeRF -name "*.so"` → **无结果** | 编译 `raymarching/` `gridencoder/` `shencoder/` `freqencoder/` |
| 4 | **OpenFace AU45 眨眼数据缺失** | `process.py` 9 个 task 里**不含**眨眼检测；全目录无 openface 字样 | 装 OpenFace 跑 `FeatureExtraction`，产出 `au.csv` 改名放入数据目录 |
| 5 | **`process.py:21` 路径大小写 bug** | 写的是 `python nerf/asr.py`，实际目录是 `NeRF/`（大写） | 改成 `NeRF/asr.py`，或软链 `ln -s NeRF nerf` |
| 6 | **`Options` 缺 `path` 字段** | `TFG/NeRFTalk.py:18-89` 的 Options 无 `path`，而 `provider.py:395` 的训练集要消费它 | 扩 `Options` 或写独立入口 |

#### 4.4.3 数据要求（这是真正的门槛）

| 项 | 要求 |
|---|---|
| 视频 | **单人、25 FPS、约 512×512、1~5 分钟、每帧都有人脸**（`README.md:685-687` 说「五分钟左右的视频」） |
| 预处理耗时 | **数小时**（抽帧 / face parsing / 背景 / torso / landmark / 3DMM tracking / transforms） |
| 额外依赖 | TensorFlow、PyTorch3D、OpenFace |
| 输出 | `transforms_train.json` / `transforms_val.json` / `aud.wav` / `ori_imgs/` / `parsing/` / `gt_imgs/` / `torso_imgs/` |
| 训练 | `Trainer.train(train_loader, valid_loader, max_epochs)`（`utils.py:967`） |

#### 4.4.4 工作量诚实评估

| 阶段 | 估时 | 风险 |
|---|---|---|
| 装 pytorch3d + 编译 4 个 CUDA 扩展 | 半天 | 编译失败常见（CUDA 版本、ninja） |
| 装 OpenFace + 跑 AU45 | 半天 | OpenFace 是老 C++ 项目，环境难搞 |
| 补 `main.py` + `Options.path` | 1 天 | 中等，需读懂 `provider.py:387-485` |
| 拍/剪 5 分钟合规视频 | **看你** | 需真人配合，25fps 512×512 全程露脸 |
| 预处理 | **数小时机器时间** | 可能 OOM / 中途失败要重跑 |
| 训练 | 数小时~数天 | 需调参，效果不保证 |

> **总投入：数天工程 + 数小时~数天机器时间 + 一个愿意配合拍视频的人。**

#### 4.4.5 关键判断：电商直播该不该训形象？

**大概率不该。** 理由：

| 维度 | SadTalker（零训练） | ER-NeRF（要训练） |
|---|---|---|
| 头部姿态 | 有，跟随音频 | 更精细 |
| 牙齿/口腔细节 | 一般 | 更好 |
| **能换衣服/换背景吗** | ✅ 换图即可 | ❌ **训练后形象就锁死了** |
| **直播要换装、换场景、上贴片** | ✅ 灵活 | ❌ 每换一次都要重训 |
| 新主播上线成本 | 传一张图 | 数天 |
| 成本 | 0 | 数天 + 出镜配合 |

**电商直播的核心诉求是「快速试、快速换」**——换主播、换品类、换场景、做 A/B。ER-NeRF 的「锁死」特性和这个诉求**方向相反**。

**建议**：把预算投到 §5 的**话术与互动系统**上——那才是直播间留人的关键，也是当前**完全空白**的部分。形象训练留到「话术已经跑通、且明确知道 SadTalker 的画质是瓶颈」之后再考虑。

---

## 5. 电商直播到底缺什么（这才是真正的差距）

### 5.1 能力对照

| 电商直播需要 | 当前系统 | 差距 |
|---|---|---|
| 数字人出镜口播 | ✅ SadTalker/Wav2Lip/MuseTalk | — |
| 专属音色 | ✅ GPT-SoVITS 微调 | — |
| 形象/音色资产化 | ✅ **本文档 §2 已补** | — |
| **讲品话术生成** | ❌ **LLM 未接线** | 🔴 大 |
| **商品知识** | ❌ **无** | 🔴 大 |
| **互动答疑** | ❌ **无** | 🔴 大 |
| **直播编排（循环/分段）** | ❌ **无** | 🟡 中 |
| 长时推流 | ⚠️ ffmpeg 支持 rtmp 但无编排 | 🟡 中 |
| 吞吐 | ⚠️ **实测 17~42 秒/条** | 🟡 中 |

> **吞吐实测**：冷启动时 SadTalker 生成一条短句**耗时 41.9 秒**（模型首次加载）；模型预热后 **17~18 秒**。
> 规划产能时**按 42 秒算**（保守），因为服务重启、切换引擎都会让模型回到冷态。

### 5.2 缺口 1：LLM 完全没接线

`LLM/` 目录下有 **12 个实现**（ChatGLM / Qwen / Qwen2 / Linly / Llama2Chinese / ChatGPT / Gemini / GPT4Free / QAnyThing…），**全部未接线**。

- 没有任何接口能调到它们。`llm.py` 这个文件名都不存在，实际是 `LLM/` 包。
- 人设 `system_input` 只在 Gradio 界面里是自由文本，**不持久化**（`webui.py:528`），关掉页面就没了。
- **人设与形象完全无关**：切「女性角色」（`webui.py:450-451` → `character_change` `:380-389`）只改图片和默认音色，**不动 system prompt**；改 prompt 也不影响形象。**没有任何「人设 + 形象」的联合配置对象。**
  > ✅ 这正是 §2.2 把 `persona` 放进档案要解决的问题。

**好消息：接线成本很低。**

- `openai` 包**已装**（3.13.0），`LLM/ChatGPT.py` 已有调用骨架（但用的是旧版 `openai.ChatCompletion.create` API，要改成 v1 的 `client.chat.completions.create`）。
- 本地还有 `Qwen/Qwen-1_8B-Chat` 权重（3.5 GB，在 `Linly-Talker/Qwen/`），离线可跑。
- 任何 OpenAI 兼容端点（DashScope / DeepSeek / 智谱 / vLLM）都能直接接。

**建议**：不走 Linly-Talker 的 `LLM/` 包（那是给 Gradio 对话设计的），**在 `linly-api/` 里新写一个 `app/script_gen.py`**，用 `openai` 客户端打一个兼容端点。理由：服务层允许改，仓库保持零改动。

### 5.3 缺口 2：没有商品知识

直播间不能胡说。需要一个**商品库**，放进档案目录（`products.json`）：

```json
{
  "products": [
    {
      "sku": "MZ001",
      "name": "水润保湿精华 30ml",
      "price": 129,
      "origin_price": 199,
      "selling_points": ["玻尿酸原液", "敏感肌可用", "四季通用"],
      "specs": {"容量": "30ml", "保质期": "3年", "产地": "上海"},
      "forbidden": ["不能说\"根治\"", "不能用\"最\"字"],
      "faq": [
        {"q": "敏感肌能用吗", "a": "可以，配方无酒精无香精。"},
        {"q": "多久见效", "a": "建议连续使用 28 天，配合日常保湿。"}
      ]
    }
  ]
}
```

**用法**：生成话术时把相关商品塞进 prompt 作为**唯一事实来源**，并显式约束「不得编造参数」。`forbidden` 字段直接映射到广告法合规要求。

> ⚠️ **`forbidden` 不是可选项。** 电商直播受《广告法》约束（「最」「第一」「根治」「国家级」等绝对化用语），必须有拒答/改写机制。

### 5.4 缺口 3：没有直播编排

24 小时无人直播不是「生成一条长视频」，而是**循环播放一组短片**：欢迎语 → 讲品 A → 讲品 B → 促单 → 回到欢迎语。

**可行的做法（不需要改 Linly-Talker）**：

```
直播脚本 (script.json)
   ↓
批量调 6006 API 渲染成 N 个 mp4
   ↓
ffmpeg concat 拼成播放列表
   ↓
ffmpeg 推流到 RTMP
```

**推流能力已具备**（实测）：ffmpeg 4.2.2 带 `rtmp` / `rtmps` / `hls` 协议 + `libx264` + `aac` 编码器。

### 5.5 缺口 4：吞吐是硬约束——决定了你能做什么形态的直播

**实测 17~42 秒/条**（视模型冷热）。单卡串行（`MAX_WORKERS=1`，`config.py:33`），因为「单卡只能串行跑 GPU 推理」。

| 直播形态 | 可行？ | 说明 |
|---|---|---|
| **无人直播（预生成 + 循环推流）** | ✅ **完全可行** | 50 条短片 ≈ 35 分钟渲染（按 42 秒保守算）。今天就能做 |
| **半自动（定时换品、人工介入）** | ✅ 可行 | 预生成 + 后台补片 |
| **实时互动（弹幕→回答→出片）** | ❌ **撑不住** | 弹幕到画面**至少 17 秒**，冷态 42 秒，且会堵死队列 |

> 🔴 **不要按「实时互动直播」设计。** 单 worker 串行意味着任何实时互动都会瞬间积压。这是**架构约束**（单卡、串行），不是调参能解决的。
>
> 想做实时的唯一路径：**预生成常见问题的回答短片**（用商品库 `faq` 生成），直播时按弹幕匹配 → 直接切已渲染好的片段。把「实时」降级成「预渲染 + 快速切换」，延迟降到亚秒级。

### 5.6 建议架构

```
┌─ 离线（慢，无所谓）──────────────────────────────┐
│  商品库 products.json                            │
│    ↓                                             │
│  LLM 生成话术 (script_gen.py)                    │
│    ↓                                             │
│  数字人档案 (profile.json) ──→ 批量渲染 N 条 mp4   │
│    ↓                                             │
│  ffmpeg concat 拼播放列表                        │
└──────────────────────────────────────────────────┘
                    ↓
┌─ 在线（快，不能卡）──────────────────────────────┐
│  ffmpeg 推流 RTMP → 抖音/淘宝/视频号             │
│  （可选）弹幕匹配 → 切预渲染 FAQ 片段            │
└──────────────────────────────────────────────────┘
```

---

## 6. 分阶段实施方案

### 阶段 0：资产固化（**先做这个**，1~2 天）

**目标**：把已确认的形象和音色存下来，不再受 6 小时 TTL 威胁。

```bash
# ① 如果还没有音色，先训一个（可选；也可先用 edge 音色跑通流程）
KEY=$(cat /root/autodl-tmp/linly-api/.api_key)
curl -s -X POST "http://127.0.0.1:6006/api/v1/voices" -H "X-API-Key: $KEY" \
  -F "audio=@/path/to/录音.wav" -F "voice_id=xiaomei_voice" -F "auto_train=false"
# → 轮询到 reviewing → 取 transcript → 人工校对 → 提交训练

# ② 建档案
cd /root/autodl-tmp/digital-humans
python dh.py create --id xiaomei --name "小美·美妆主播" \
  --image /root/autodl-tmp/Linly-Talker/inputs/girl.png \
  --voice-id xiaomei_voice

# ③ 校验 + 出试片验收
python dh.py verify xiaomei
python dh.py render xiaomei --text "欢迎来到我的直播间"

# ④ 备份
python dh.py export xiaomei --with-weights --out /root/autodl-tmp/backup/xiaomei.tar.gz
```

**验收线**：`dh.py verify` 三项全绿 + 试片 **260~370 KB**（实测 420~442 KB 也正常；低于 200 KB 说明踩了 `crop` 坑）。

### 阶段 1：形象+音色定版（0.5 天）

- 用 `dh.py render` 出 3~5 个不同角度的试片，确认形象在**竖屏构图**下可用
- 音色做 A/B：`finetuned` vs 零样本 `gptsovits`（见 `音色克隆训练方案.md` §7.2）
- 定版后 `version` 记 1，`preview.mp4` 存档

### 阶段 2：直播话术系统（✅ **已实现**，见 `live-commerce/`）

**代码位置**：`/root/autodl-tmp/live-commerce/`（仓库外，零侵入）。见该目录 `README.md`。

```
products.json  →  script_gen.py  →  script.json  →  live_build.py  →  playlist.mp4
   商品库            话术生成          话术稿         批量渲染+拼接       播放列表
                        ↑
                  compliance.py（广告法拦阻，贯穿生成与渲染两环节）
```

| 组件 | 状态 | 说明 |
|---|---|---|
| `compliance.py` | ✅ 实测 | 广告法拦阻。block/warn 两级；能抓 `最 好`、全角 `１００％` 等规避写法；重叠命中按长词优先去重 |
| `products.example.json` | ✅ | 3 个 SKU 示例，字段结构即生产 schema |
| `script_gen.py` | ✅ 实测 | `--backend llm\|template` **双后端**，输出同一 schema，可先跑通再换 LLM |
| `live_build.py` | ✅ 实测 | 批量渲染 + ffmpeg 拼接。**可断点续跑**、渲染完立刻落盘（躲 6h TTL） |
| `llm_config.example.json` | ✅ | LLM 配置模板，**不含真实 key** |

**LLM 接线方式**（成本很低）：任何 OpenAI 兼容端点都行（DeepSeek / 通义 / 智谱 / 月之暗面 / 本地 vLLM）。
`openai` 包已装（3.13.0，标准 v1 API）。配置走 `llm_config.json` 或环境变量
`LIVE_LLM_API_KEY` / `LIVE_LLM_BASE_URL` / `LIVE_LLM_MODEL`。

> 🔑 **合规是这套流水线的重点，不是附属功能。** 生成时 block 级违规会让 LLM 自动重写（最多 2 轮），
> 仍违规就**拒收，不写进 `script.json`**——违规话术进不了渲染环节。
>
> 📌 实测中 `compliance.py` **抓出了示例商品数据里的真实问题**（`"100% 聚酯纤维"`），
> 并借此校准了规则：「100% 纯棉」是成分含量陈述（属实即合法），「100% 有效」才是功效绝对化——
> 两者语境不同，一刀切会误伤正常商品描述。所以该词判 **warn 而非 block**。

**推流**（自行替换地址）：

```bash
ffmpeg -re -stream_loop -1 -i build/<profile>/playlist.mp4 \
  -c copy -f flv rtmp://<你的推流地址>
```

| 步骤 | 产出 | 说明 |
|---|---|---|
| 2.4 接推流 | RTMP 出流 | 需各平台开放平台授权拿推流地址 |

> 🔴 **实测结论：不要在这台 AutoDL 上推流。**
> 向 Cloudflare 测速端点上传 10 MB × 5 次，上行 **1.1 ~ 25.5 Mbps，波动 23.4 倍**，
> 5 次里有 2 次低于 2 Mbps。而 720p 推流需要**持续稳定**的 2~4 Mbps。
> ⇒ **渲染留在 AutoDL，推流放到有稳定带宽的机器上**（本地机 / 轻量云服务器）。
> 这个分离其实更合理：AutoDL 只做它擅长的 GPU 批量渲染，
> 顺带解决「实例关机就断播」的问题。
> 详见 `live-commerce/FAQ弹幕匹配实现计划.md` §8.1。

**2.2 的 prompt 骨架**：

```python
SYSTEM = """你是电商直播间的脚本策划。只使用我给你的商品资料，绝不编造参数。
禁止使用绝对化用语（最、第一、根治、国家级、100%）。
每段口播 60~120 字，口语化，适合直接朗读。
输出 JSON 数组：[{"sku": "...", "text": "..."}]"""
```

**2.3 的关键约束**：
- 批量提交时**串行等待**（单 worker，别并发提交）
- 每条按 **42 秒** 估算，**50 条 ≈ 35 分钟**
- 渲染完**立刻下载并归档**——产物有 6 小时 TTL

### 阶段 3（可选）：形象训练

只有满足以下**全部**条件才做：
1. 阶段 2 已跑通，直播有稳定产出
2. 明确知道画质是瓶颈（而不是话术/选品）
3. 有真人愿意配合拍 5 分钟 25fps 512×512 视频
4. 接受「**形象锁死、换装要重训**」

否则**不做**。理由见 §4.4.5。

### 阶段 4（可选）：实时互动

预渲染 FAQ 片段（从 `products.json` 的 `faq` 生成）→ 弹幕关键词匹配 → 切换片段。**不要试图实时生成。**

---

## 7. 运维与风险

### 7.1 显存

| 项 | 占用 | 说明 |
|---|---|---|
| 每个微调音色常驻 | **约 240 MB** | `_gpt_sovits` 缓存**无上限无淘汰**（`adapter.py:561-587`） |
| BERT + HuBERT | 一份共享 | 不随音色数增长 |
| 模型加载 | 惰性 | 首个请求慢 1~3 分钟 |

> ⚠️ **10 个音色 ≈ 2.4 GB 常驻**，加上 SadTalker/MuseTalk 权重，24 GB 会紧张。
> **规避**：不要长期保留不用的音色；重训音色**换新 `voice_id`**（同时规避 §3.5 的缓存陷阱）。

### 7.2 磁盘

现状：**50G 已用 27G，可用 24G**。

| 可释放 | 大小 | 说明 |
|---|---|---|
| `Linly-Talker/Qwen/` | **3.5 G** | LLM 权重，**未接线**。若改用云端 LLM 就打水漂了，可删 |
| `conda/pkgs/` | 645 M | `conda clean -a` |
| `Linly-Talker/results/` | 27 M | 历史产物 |

> 📌 **阶段 3 做 NeRF 训练的话，磁盘会告急**：预处理产物对 5 分钟 25fps 视频是 **7500 帧 × 多份**（`ori_imgs/` + `parsing/` + `gt_imgs/` + `torso_imgs/`），轻松几十 GB。**必须先腾出 ≥ 20G。**

### 7.3 已知未修的问题（扩展前需知）

| # | 问题 | 位置 |
|---|---|---|
| 1 | `MAX_AUDIO_BYTES`(100MB) 被 `MAX_REQUEST_BYTES`(20MB) 中间件**实际截断**，报错文案却写 20MB | `config.py:43,51` vs `main.py:143-147` |
| 2 | `TRAIN_TTL_SECONDS` / `TRAIN_MAX_CONCURRENCY` **定义后从未使用** → 音色无过期清理、训练无并发闸门（并发提交两个 `voice_id` 会起两个训练进程抢显存） | `config.py:66,68` |
| 3 | `DELETE /api/v1/voices/{id}` **不删** `uploads/` 里的源音频 → 孤儿文件 | `voices.py:288-294` |
| 4 | `adapter.VOICES_ROOT` 硬编码，与 `config.VOICES_DIR` 是**两份同值来源** | `adapter.py:49` vs `config.py:63` |
| 5 | `GET /api/v1/files/{filename}` **无鉴权**，仅靠归属校验兜底 | `main.py:367-380` |
| 6 | `/docs` `/redoc` **未关闭**，公网可查完整接口结构 | `main.py:126-131` |
| 7 | 服务进程 CWD 被改成项目仓库根，相对路径写入靠 `_cleanup_previous` 事后收拾 | `adapter.py:156`、`:687-717` |

**做电商直播前建议至少修掉 #5 #6**（公网暴露）。

### 7.4 合规提醒（电商直播特有）

- **AI 生成内容需标识**：《互联网信息服务深度合成管理规定》要求对深度合成内容进行标识。直播间应显著标注「AI 数字人主播」。档案里的 `persona.disclosure` 字段就是为这个留的。
- **广告法**：绝对化用语（「最」「第一」「根治」「国家级」）禁用。必须在 `products.json` 的 `forbidden` 里显式列出并做拦阻。
- **声音与肖像授权**：只克隆**你有权使用**的声音，只用**你有权使用**的肖像。上游作者明确说明了不提供克隆语音权重的原因就是版权（`常见问题汇总.md:98-101`）。用于商业直播尤其要注意——**这是法律风险，不是技术风险**。
- **直播回放**也是一份资产，注意留存与合规。

---

## 8. 一页速查

```bash
# ═══ 数字人档案 ═══════════════════════════════════════
cd /root/autodl-tmp/digital-humans

# 建档案（形象图 + 训好的音色）
python dh.py create --id xiaomei --name "小美·美妆主播" \
  --image face.png --voice-id xiaomei_voice --engine sadtalker

# 建档案（还没训音色，先用 edge）
python dh.py create --id demo --image face.png \
  --voice-engine edge --speaker zh-CN-XiaoxiaoNeural

# 校验三件套（形象 sha256 + 音色权重 + 参考音频）
python dh.py verify xiaomei

# 出片
python dh.py render xiaomei --text "欢迎来到我的直播间"

# 列出 / 查看 / 迁移
python dh.py list
python dh.py show xiaomei
python dh.py export xiaomei --with-weights --out /tmp/xiaomei.tar.gz

# ═══ 资产位置 ═════════════════════════════════════════
# 数字人档案     /root/autodl-tmp/digital-humans/<id>/
# 音色权重       /root/autodl-tmp/voices/<voice_id>/{gpt.ckpt,sovits.pth,meta.json}
# 训练语料       /root/autodl-tmp/linly-api/voice_work/<voice_id>/
# 服务代码       /root/autodl-tmp/linly-api/
# 训练流水线     /root/autodl-tmp/voice-train/pipeline.py

# ═══ 服务 ═════════════════════════════════════════════
# 启动（必须带 bash，run.sh 无可执行位）
nohup bash /root/autodl-tmp/linly-api/run.sh > /root/autodl-tmp/logs/api.log 2>&1 &
# 停止（方括号！）
pkill -f "[u]vicorn app.main:app"
# 健康检查（看 pipeline_impl 是否 linly-talker）
curl -s localhost:6006/api/v1/health

# ═══ 数字人的 voice 串（档案会自动拼）═════════════════
# finetuned:<音色名>|<参考音频>|<参考文本>|中文
# gptsovits:<参考音频>|<参考文本>|中文
# cosyvoice:中文女        edge:zh-CN-XiaoxiaoNeural

# ═══ 验收线 ═══════════════════════════════════════════
# 成片 260~370 KB（实测 420~442KB 亦正常）/ 640x1024
# 低于 200 KB 说明踩了 crop 坑
# 单条耗时：预热 17~18 秒，冷态 42 秒 —— 按 42 秒规划产能

# ═══ 三条硬约束（音色）════════════════════════════════
# 1. 权重路径绝不能含 "pretrained" 子串
# 2. 参考音频必须 3~10 秒、服务器本地路径
# 3. 推理语言写「中文」，训练 list 写「zh」——别混
```

---

## 附：本文档的核实方式

| 结论 | 核实方式 |
|---|---|
| 形象零训练 | 读 `TFG/SadTalker.py:182-207` 确认 `test2` 直接吃图片路径，无训练产物依赖 |
| 6 小时 TTL 删图 | `queue.py:179-180` 删 `result_path` **和** `image_path` |
| ER-NeRF 训练代码完整 | 实读 `NeRF/nerf_triplane/utils.py:568/718/967/1057/1217/1403/1465`、`gui.py:72/140`、`provider.py:387` |
| 缺 CLI 入口 | `tail gui.py` 确认末尾为 `render()`，无 `__main__`/`argparse` |
| pytorch3d 缺失 | `python -c "import pytorch3d"` → `ModuleNotFoundError` |
| CUDA 扩展未编译 | `find NeRF -name "*.so"` → 无结果 |
| `process.py` 路径 bug | 实读 `process.py:21` = `python nerf/asr.py`（小写），实际目录 `NeRF/` |
| 无 OpenFace | `grep -rn "openface\|AU45" NeRF/data_utils/` → 无命中 |
| **42 秒/17 秒** | `GET /api/v1/tasks` 读回真实 `elapsed_seconds`（41.9 冷 / 17.4 热） |
| 推流可行 | `ffmpeg -protocols` 含 `rtmp/rtmps/hls`，`-encoders` 含 `libx264/aac` |
| LLM 未接线 | `LLM/` 12 个实现，`grep` 无任何调用路径；使用手册 §9 已确认 |
| 无档案概念 | `grep -rn "asset\|profile\|preset\|digital_human" linly-api/app/` → 无命中 |
| **`dh.py` 可用** | 全子命令实机跑通；产出 640×1024 h264+aac，PCM 峰值 24425（非静音） |
| **音色训练 88.1 秒** | `GET /api/v1/voices/dh_smoke` 读回真实 `elapsed_seconds` |
