# 电商直播数字人项目 · 部署进度总览

> **本文档定位**：全项目的**进度看板与索引入口**。回答「做到哪了、还差什么、卡在哪」。
>
> **不是**部署过程记录（那份在 `部署进度与交接.md`，1258 行，叙事体），
> **也不是**操作手册（那份在 `Linly-Talker使用手册.md`）。
>
> 核验日期：**2026-09-16 22:30**（**2026-09-17 更新**：§一 / §4.1 B1 / §7 ① —— LLM Key 已通）。
> 本文档所有状态均为**实机核实**，非推测。
>
> ⚠️ **接手必读**：AutoDL 切换有卡/无卡模式会清空聊天记录，本文档是项目级状态的唯一可靠载体。

---

## 一、一句话现状

**能跑通的**：输入一张人脸图 + 一段文本，产出 640×1024 的说话视频；能用自己训练的专属音色；
能批量渲染成 4 分钟直播播放列表；能实时接收弹幕并把画面无缝切到对应的预渲染回答片段。

**卡住的**：真实推流（AutoDL 上行带宽不达标，需另找推流机）。

> ✅ **2026-09-17 更新**：接 LLM 出真话术**已通**。原先的「缺 API Key」阻塞项（B1）已解除，
> 实测 `--backend llm` 正常出稿（9 段 / 合规全绿）。排查过程与踩的坑见 §4.1.1。

**离「能开播」还差**：真实形象图、真实音色、真实商品数据、推流机 —— **全部在你这侧**，代码侧已就绪。

---

## 二、环境与路径速查

| 项 | 值 |
|---|---|
| GPU | RTX 4090 / 24564 MiB（**当前占用 6125 MiB**） |
| 磁盘 | 50G 中已用 28G，**可用 23G** |
| conda 环境 | `/root/autodl-tmp/conda/envs/linly`（Python 3.10 + PyTorch 2.4.1+cu121） |
| 项目代码 | `/root/autodl-tmp/Linly-Talker`（HEAD `dc831b3`，**`git status` 始终干净**） |
| 公网入口 | 6006: `<你的实例地址>`（**单 u**）<br>6008: `<你的实例地址>`（**双 u**） |

**仓库外代码**（全部不影响 `git status`）：

| 目录 | 职责 | 规模 |
|---|---|---|
| `linly-api/` | FastAPI 服务（6006） | 10 文件 / 2534 行 |
| `voice-train/` | GPT-SoVITS 音色训练流水线 | 6 文件 / 931 行 |
| `digital-humans/` | 数字人档案管理 `dh.py` | 1 文件 / 459 行 |
| `live-commerce/` | 话术 → 渲染 → 弹幕匹配 | 7 文件 / 1980 行 |
| `deploy/` | 部署与验收脚本 | 13 文件 / 1108 行 |

**当前资产**：
- 音色：`/root/autodl-tmp/voices/dh_smoke/`（⚠️ **合成测试音色**，非产品音色）
- 数字人档案：`/root/autodl-tmp/digital-humans/xiaomei/`（⚠️ 用的是演示图 `girl.png`）

---

## 三、已完成事项

### 3.1 基础部署 ✅（2026-09-12 ~ 09-14）

| 项 | 状态 | 证据 |
|---|---|---|
| PyTorch 环境 | ✅ | torch 2.4.1+cu121，`is_available()=True` |
| 模型权重 | ✅ | 123 文件 / 17.67 GB，对照官方清单 **逐个 SHA256 通过** |
| 依赖安装 | ✅ | `requirements_webui.txt` 69 项 satisfied |
| mm* 系列 | ✅ | mmcv 2.1.0 **含 CUDA ops**，GPU 上实跑 NMS 通过 |
| 全量导入体检 | ✅ | 6 组 47 个模块 + MuseTalk 关键点链路，**0 失败** |

> 部署过程踩了 **12 个坑**，全部记录在 `部署进度与交接.md` 第四节。最值得记住的：
> `OMP_NUM_THREADS=0` 非法、mmcv 编译必须装 ninja（否则 2 分 41 秒变 2 小时）、
> basicsr 会白下 1.6GB torch。

### 3.2 服务层 ✅（2026-09-14）

| 服务 | 端口 | 状态 |
|---|---|---|
| FastAPI | 6006 | ✅ **正在运行**（PID 436946），`pipeline_impl: linly-talker`，`queue_depth: 0` |
| Gradio WebUI | 6008 | ✅ **正在运行**（PID 7806） |

**端到端验收**：HTTP 提交 → 轮询 → 下载，产出 **640×1024 / 248~442 KB** 的 mp4。

**架构约束（不可违反）**：FastAPI 独占 6006（AutoDL 只映射 6006/6008，而 Gradio 默认也占 6006）→
**不启动项目自带的 Gradio**，服务代码全部放仓库外 → 项目源码零改动。

### 3.3 音色训练 ✅（2026-09-16）

**上传音频 → 自动切片/ASR → 人工校对 → 训练 → `voice=finetuned:<名>` 使用**，全流程实测通过。

| 项 | 值 |
|---|---|
| 实现位置 | `voice-train/pipeline.py` + `linly-api/app/voices.py` |
| 产物 | `/root/autodl-tmp/voices/<名>/{gpt.ckpt, sovits.pth, meta.json}` |
| **实测耗时** | **88.1 秒**（16 段 / 80 秒语料 / 4+4 轮） |
| 端点 | `POST /api/v1/voices` 等 **7 个** |
| 仓库改动 | **零**（缺口用一个仓库外的 i18n 空壳补掉） |

> ⚠️ **关键认知**：Linly-Talker 自带的 GPT-SoVITS **训练链路被上游摘掉了**（缺 `tools/` 包、
> config 缺训练键、数据拼装缺失）。但**不需要去上游仓库**——补一个十几行恒等空壳 + 用仓库
> 自带的 train 脚本就能跑通。`音色克隆训练方案.md` 的旧结论已被实测推翻，见该文档顶部说明。

### 3.4 数字人档案 ✅（2026-09-16）

把「形象 + 音色 + 人设」固化成可校验、可迁移的档案。**此前系统里根本没有「保存数字人」这个概念**
——上传的图 6 小时后会被 `queue.py:179-180` 连记录一起删掉。

| 项 | 值 |
|---|---|
| 工具 | `/root/autodl-tmp/digital-humans/dh.py` |
| 档案结构 | `<档案>/{profile.json, avatar.*, ref.wav, ref.txt, preview.mp4}` |
| 子命令 | `create / list / show / verify / render / export` **全部实测通过** |
| 核心设计 | 形象 sha256 校验、`preprocess` 锁定 `full`、**参考音频复制进档案**（补掉音色不自包含缺口） |

### 3.5 直播流水线 ✅（2026-09-16）

```
products.json → script_gen.py → script.json → live_build.py → playlist.mp4
                     ↑
               compliance.py（广告法拦阻）
```

| 组件 | 状态 | 说明 |
|---|---|---|
| `compliance.py` | ✅ | 广告法拦阻，block/warn 两级；能抓 `最 好`、全角 `１００％` 等规避写法 |
| `script_gen.py` | ✅ | `--backend llm\|template` **双后端**，输出同一 schema |
| `live_build.py` | ✅ | 批量渲染 + 拼接，**可断点续跑** |

**实测**：25 段 → **4.3 分钟成片**（255.06s，与片段之和 **零偏差**），17.5 MB，耗时 11.2 分钟。

### 3.6 FAQ 弹幕匹配 ✅ 阶段一（2026-09-16）

```
danmaku.py（清洗）→ matcher.py（关键词）→ player.py（FIFO喂流）→ RTMP
                            ↑
                      faq_build.py（预渲染 + 索引）
```

| 组件 | 状态 | 说明 |
|---|---|---|
| `faq_build.py` | ✅ | 13 条 FAQ 片段 / 100 秒 / 约 6 分钟，增量渲染 + 合规复检 |
| `matcher.py` | ✅ | **自测 19/19 通过**。三档加权关键词 + 冷却 + 全局节流 |
| `danmaku.py` | ✅ | 13 类清洗用例全通过（去广告/表情/@、违规词不响应） |
| `player.py` | ✅ | 常驻 ffmpeg + FIFO，**插播响应 ≈ 55ms** |

**端到端验收**（3 条弹幕）：

```
[ 8.1s] 💬 命中「敏感肌能用吗」→ MZ001-faq1（37分）
[17.8s]    ↳ 插播完成
[22.5s] …  未命中「这个多少钱」      ← 商品库确实没有价格 FAQ，正确不答
[32.4s] 💬 命中「孕妇可以用吗」→ MZ001-faq3（30分）
```

**产物质量**：视频 **0 解码错误**、**0 损坏帧**、音轨全程无断点。

---

## 四、未完成事项

### 4.1 剩余阻塞项 —— 全部在你这侧，代码侧已就绪

| # | 事项 | 卡在哪 | 需要的动作 |
|---|---|---|---|
| ~~**B1**~~ | ~~LLM 接真话术~~ ✅ **已通（2026-09-17）** | — | **已完成，无需动作**。实测出 9 段真话术，详见 §4.1.1 |
| **B2** | **真实推流** | 🔴 **AutoDL 上行带宽不达标** | 确定一台推流机并测其上行（方法见 §5） |
| **B3** | **平台推流地址** | 需各平台开放平台授权 | 抖音/淘宝/视频号 各自的开放平台申请 |
| **B4** | **弹幕接入** | 需你决策接入方式 | 官方开放平台（需资质）/ 第三方库（有风险）/ 人工输入 |
| **B5** | **真实商品数据** | 现在是示例数据 | 给商品资料，或按 schema 自行填 `products.json` |
| **B6** | **真实形象图** | 现在用演示图 `girl.png` | 提供竖版人像图（要求见 `dh.py` 文档 §2.5） |
| **B7** | **真实音色** | 现在是**合成 TTS 测试音色** `dh_smoke` | 录 5~30 分钟干净人声，走 `POST /api/v1/voices` |

> 📌 **B2 是唯一的技术性阻塞**，其余都是资源/决策问题。
> AutoDL 上行实测 **1.1~25.5 Mbps，波动 23.4 倍**，5 次里 2 次低于 2 Mbps，
> 而 720p 推流需要**持续稳定**的 2~4 Mbps。⇒ **渲染与推流必须分离**。
> 详见 `live-commerce/FAQ弹幕匹配实现计划.md` §8.1。

#### 4.1.1 B1 已通：`llm_config.json` 的坑（2026-09-17）

已配好并**实跑验证**（DeepSeek `deepseek-chat`，`--sku MZ001 --backend llm` → 9 段全绿）。

> ⚠️ **踩过的坑，值得记住**：第一次配 key 时，粘贴混入了一个**裸换行**，文件变成
> `"api_key": "sk-<32位>\nexport",` —— JSON 字符串里含字面 LF，**解析直接失败**。
>
> 但这个失败**极难定位**：`script_gen.py:49-53` 解析失败只打一行 `⚠️ 解析失败，忽略` 就继续跑，
> `api_key` 保持 `None`；直到 `script_gen.py:196` 才抛 **`缺少 LLM API Key`** ——
> **报错指向「没配 key」，真因却是「配了但文件是坏的」**，方向完全反的。
>
> **判据**：排查「配了 key 却不生效」时，**先 `json.loads` 验文件本身能否解析**，
> 别信下游那句「缺少 api_key」。唯一可靠的生效证据是**实跑一次 LLM 后端**，
> 看 health / 加载日志都不算数。
>
> 另：`.gitignore` 原先只挡 `llm_config.json`，**挡不住修复时留的 `llm_config.json.broken` 备份**
> （同样含密钥）。已改为 `llm_config.json.*` + `!llm_config.example.json`。

### 4.2 🟡 待办 —— 无阻塞，可直接做

| # | 事项 | 工作量 | 说明 |
|---|---|---|---|
| **T1** | 修复插播点的 PTS 不连续 | 约 1 天 | 每次插播产生约 46 条 DTS 警告（**产物仍可正常解码**）。需轻量 TS 解析器改写时间戳。`-avoid_negative_ts` 等参数实测**均无效** |
| **T2** | ffmpeg 进程自愈 | 半天 | 播放器已有重启逻辑，需补长跑验证 |
| **T3** | HTTP / WebSocket 弹幕输入 | 半天 | 现在只有 stdin；接口已定义好，加个端点即可 |
| **T4** | 向量兜底匹配（阶段三） | 2~3 天 | 关键词覆盖不了的换句式提问。**需先攒真实弹幕**再调阈值 |
| **T5** | `linly-api` 的 7 条已知缺陷 | 1~2 天 | 见 §6 台账，其中 **#5 #6 是公网安全问题，建议优先** |
| **T6** | 连续推流稳定性测试 | — | 阻塞于 B2/B3 |

### 4.3 ⚪ 评估后建议**不做**

| 事项 | 为什么不做 |
|---|---|
| **ER-NeRF 形象训练** | 训练代码**完整存在**（`Trainer` / `NeRFGUI` / `NeRFDataset` / 预处理链路齐全），缺的只是 CLI 入口 + pytorch3d + CUDA 扩展 + OpenFace，约**数天工程**。但 **ER-NeRF 训完形象就锁死**，换装/换背景/换场景都要重训——**与电商直播「快速试、快速换」的诉求方向相反**。详见 `数字人资产固化与电商直播方案.md` §4.4.5 |
| **实时互动直播** | 单条渲染 17~42 秒、单 worker 串行，**任何「弹幕→实时出片」的链路都会瞬间积压**。只能走「预渲染 FAQ + 匹配切换」（即已实现的方案） |

---

## 五、关键实测数据汇总

> 全部为**本机实测**，非估算。规划产能时以此为准。

| 指标 | 值 | 用途 |
|---|---|---|
| 单条口播渲染 | **42 秒（冷态）/ 17~18 秒（预热）** | 产能规划按 42 秒算 |
| 音色训练 | **88.1 秒**（16 段/80 秒语料/4+4 轮） | — |
| 25 段成片 | **11.2 分钟** → 4.3 分钟播放列表 | 无人直播产能 |
| 13 条 FAQ 预渲染 | **约 6 分钟** | 弹幕库建设 |
| **弹幕插播响应** | **≈ 55 ms** | 交互体验 |
| ffmpeg 启动开销 | **1.8 秒** | 所以不能 kill/restart 切片段 |
| **AutoDL 上行带宽** | **1.1~25.5 Mbps（波动 23.4×）** | 🔴 **不能推流** |
| 成片规格 | 640×1024 / 248~442 KB | 验收线 260~370 KB |
| 每音色显存 | 约 240 MB（缓存**无上限无淘汰**） | ⚠️ 10 个音色 ≈ 2.4 GB |

**复测上行带宽的方法**（换推流机后应重测）：

```bash
curl -s -o /dev/null -w "%{speed_upload}\n" --max-time 90 \
  -X POST --data-binary @<10MB文件> https://speed.cloudflare.com/__up
# speed_upload 单位是 bytes/s，×8/1e6 得 Mbps
# ⚠️ 本机没有 bc，别用 bc 算；关键看 5 次的【波动倍数】，不是中位数
```

---

## 六、已知问题与风险台账

### 6.1 `linly-api` 服务层的缺陷（未修）

| # | 问题 | 位置 | 影响 |
|---|---|---|---|
| **5** | `GET /api/v1/files/{filename}` **无鉴权**，仅靠归属校验兜底 | `main.py:367-380` | 🔴 公网暴露，**建议优先修** |
| **6** | `/docs` `/redoc` **未关闭**，公网可查完整接口结构 | `main.py:126-131` | 🔴 公网暴露，**建议优先修** |
| 1 | `MAX_AUDIO_BYTES`(100MB) 被 `MAX_REQUEST_BYTES`(20MB) 中间件**实际截断**，报错文案却写 20MB | `config.py:43,51` | 音频上限名不副实 |
| 2 | `TRAIN_TTL_SECONDS` / `TRAIN_MAX_CONCURRENCY` **定义后从未使用** | `config.py:66,68` | 音色无过期清理、训练无并发闸门（并发提交会抢显存） |
| 3 | `DELETE /api/v1/voices/{id}` **不删** `uploads/` 里的源音频 | `voices.py:288-294` | 孤儿文件 |
| 4 | `adapter.VOICES_ROOT` 硬编码，与 `config.VOICES_DIR` 是**两份同值来源** | `adapter.py:49` vs `config.py:63` | 扩展时必踩 |
| 7 | 服务进程 CWD 被改成项目仓库根，相对路径写入靠 `_cleanup_previous` 事后收拾 | `adapter.py:156`、`:687-717` | 脆弱 |

### 6.2 已解决但值得记住的坑

| 坑 | 后果 | 状态 |
|---|---|---|
| **EdgeTTS 的 VOLUME 是反的**（`TTS/EdgeTTS.py:146` 做 `volume = 100 - volume`） | 传 0 得到**格式合法、长度正常、内容全零**的静音轨，**ffprobe 查不出来**，只有量 PCM 才发现 | ✅ 适配层已固定传 100 |
| **SadTalker 的 `preprocess=crop`** | 成片恒 63 KB / 256×256「一张浮空的脸」 | ✅ API 路径已固定 `full`；⚠️ **Gradio 路径默认仍是 crop，用时要手动改** |
| **`pkill -f "uvicorn app.main:app"` 会自杀** | 自己的 shell 命令行含同样字符串 | ✅ 已改用方括号写法 `"[u]vicorn ..."` |
| **`run.sh` 没有可执行位** | `nohup .../run.sh` 以 **Exit 126 静默失败** | ✅ 一律写 `nohup bash .../run.sh` |
| **音色权重路径不能含 `"pretrained"` 子串** | `VITS/GPT_SoVITS.py:351` 走错分支 | ✅ 权重放 `/root/autodl-tmp/voices/` |

### 6.3 合规风险（非技术）

| 风险 | 说明 |
|---|---|
| **AI 生成内容标识** | 《互联网信息服务深度合成管理规定》要求标识。档案里 `persona.disclosure` 字段就是为此预留 |
| **广告法绝对化用语** | ✅ 已有 `compliance.py` 拦阻，但**规则表不可能穷尽，发布前仍需人工审核** |
| **声音/肖像授权** | 只克隆**有权使用**的声音与肖像。上游作者明确说明不提供克隆语音权重的原因就是版权 |
| **无人直播平台政策** | 各平台对无人直播的合规要求不同，可能被限流 |

---

## 七、下一步建议顺序

```
✅ 填 LLM Key            →  已完成（2026-09-17），跳过。换 key 时见下方命令
② 提供真实素材           →  真实形象图 + 真实音色 + 真实商品数据（并行）
   └─ 音色训练约 90 秒，商品 schema 见 products.example.json
③ 本地跑一场试播         →  用 build/xiaomei/loop 推本地 FLV，看真实观感
   └─ 不依赖 RTMP，现在就能做
④ 确定推流机并测上行     →  解开 B2 阻塞
⑤ 申请平台推流地址       →  解开 B3（审批可能较慢，建议早启动）
⑥ 修公网安全问题         →  linly-api 缺陷 #5 #6
⑦ 阶段二：PTS 修复 + 稳定性测试
```

**换 key 的命令**（B1 已完成，此处供后续更换 key 时照办；**别把 key 贴进对话**，会留在记录里）：

```bash
cat > /root/autodl-tmp/live-commerce/llm_config.json <<'EOF'
{
  "api_key": "sk-你的key",
  "base_url": "https://api.deepseek.com/v1",
  "model": "deepseek-chat",
  "temperature": 0.7,
  "timeout": 120
}
EOF

cd /root/autodl-tmp/live-commerce
# 【第 1 步，别省】先验 JSON 本身是否合法——粘贴混入换行会让它静默失效
python -c "import json;json.load(open('llm_config.json'));print('✅ JSON 合法')"
# 【第 2 步】唯一可靠的生效证据是实跑；看 health / 加载日志都不算数
python script_gen.py --sku MZ001 --backend llm --profile xiaomei -o /tmp/probe.json
```

⚠️ 若第 2 步报 `缺少 LLM API Key`，**先回头查第 1 步的 JSON 是否合法**，不要以为 key 没配上（真因见 §4.1.1）。
`base_url` 换成你用的那家即可（通义 / 智谱 / 月之暗面 都兼容，模板文件里有对照表）。
该文件已在 `.gitignore` 里（含修复备份 `llm_config.json.*`）。

**③ 的命令**：

```bash
cd /root/autodl-tmp/live-commerce
P=/root/autodl-tmp/conda/envs/linly/bin/python
( sleep 10; echo '{"user":"张三","text":"敏感肌能用吗"}'; sleep 30 ) | \
  $P player.py --profile xiaomei --main build/xiaomei/loop \
    --output local:/tmp/live.flv --max-seconds 40 --reset-cooldown
```

---

## 八、文档索引

| 文档 | 权威范围 | 大小 |
|---|---|---|
| **本文档** | **全项目进度看板与索引** | — |
| `部署进度与交接.md` | 初次部署全过程（**叙事体，含 12 个坑**） | 90 KB |
| `Linly-Talker使用手册.md` | 日常操作：启动 → 调用 → 调参 → 排错 | 24 KB |
| `数字人资产固化与电商直播方案.md` | 三大角度分析 + 分阶段方案 | 47 KB |
| `live-commerce/FAQ弹幕匹配实现计划.md` | 弹幕匹配设计与实测（阶段一已完成） | 24 KB |
| `live-commerce/README.md` | 直播流水线操作 | 10 KB |
| `linly-api/README.md` | 6006 服务对外契约（⚠️ 有两处已知错误，以使用手册为准） | 9 KB |
| `linly-webui/README.md` | 6008 Gradio 操作细节 | 8 KB |
| `音色克隆训练方案.md` | 音色微调原理与排错（⚠️ 旧结论已部分被推翻，见文档顶部更正） | 23 KB |
| `交付报告.md` | 2026-09-14 的部署交付验收 | 23 KB |
| `README.md` | 项目架构、模型清单、实现复盘 | 41 KB |

---

## 附：验收命令速查

```bash
# ── 服务 ──────────────────────────────────────────────
nohup bash /root/autodl-tmp/linly-api/run.sh > /root/autodl-tmp/logs/api.log 2>&1 &   # 启动（必须带 bash）
pkill -f "[u]vicorn app.main:app"                                                     # 停止（方括号！）
curl -s localhost:6006/api/v1/health    # 看 pipeline_impl 是否为 linly-talker

# ── 数字人档案 ────────────────────────────────────────
cd /root/autodl-tmp/digital-humans
python dh.py verify xiaomei             # 三项全绿才算完好
python dh.py render xiaomei --text "测试"

# ── 直播流水线 ────────────────────────────────────────
cd /root/autodl-tmp/live-commerce
python script_gen.py --list
python script_gen.py --all --backend template --profile xiaomei -o script.json
python live_build.py -i script.json --profile xiaomei --limit 2

# ── 弹幕匹配 ──────────────────────────────────────────
python faq_build.py -i script.json --profile xiaomei
python matcher.py --selftest            # 应为 19/19
python player.py --profile xiaomei --main build/xiaomei/loop \
    --output local:/tmp/live.flv --max-seconds 30 --reset-cooldown

# ── 验收线 ────────────────────────────────────────────
# 成片 260~370 KB / 640×1024（实测 420~442 KB 亦正常；低于 200 KB = 踩了 crop 坑）
# 音轨必须量 PCM 确认非静音（峰值应 >10000）
# 弹幕自测应 19/19 通过
```
