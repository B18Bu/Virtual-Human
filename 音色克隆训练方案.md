# 音色克隆训练方案（GPT-SoVITS 微调）

> ⚠️ **2026-09-16 更新：自动化流水线已实现并跑通，本文档的手工步骤已被取代。**
>
> 现在有两条路：
>
> | 方式 | 怎么做 | 适用 |
> |---|---|---|
> | **API（推荐）** | `POST /api/v1/voices` 上传音频 → 校对 → 训练 → `voice=finetuned:<名>` 使用 | 日常 |
> | **命令行** | `python /root/autodl-tmp/voice-train/pipeline.py --input x.wav --voice 名字` | 批量/调试 |
>
> 实现代码在 `/root/autodl-tmp/voice-train/`（**仓库外**，仓库仍零改动）。
> 下面 §5 的手工步骤保留作**原理说明与排错参考**——它解释了流水线内部在做什么。
>
> **§5 步骤 5 里的存放路径已作废**：那里建议放 `GPT_SoVITS/pretrained_models/<名>/`，
> 但该路径**含 `pretrained` 子串**，会触发 `VITS/GPT_SoVITS.py:351`
> 的 `if ("pretrained" not in sovits_path): del vq_model.enc_q` 走错分支。
> 正确位置是 `/root/autodl-tmp/voices/<名>/`（流水线已这么做）。

> 目标：训练一个**专属音色模型**，让 Linly-Talker 用它合成语音，效果比零样本克隆更稳、更像。
>
> 核验日期：2026-09-16。本文档中所有「本仓库跑不通」的结论均有实跑证据，见 §2。
>
> **范围声明**：上一轮任务书 `AutoDL部署提示词.md:221` 明确写了「不要训练或微调任何模型（GPT-SoVITS 微调不在本次范围）」。**本文档是新的一轮范围**，与该约束不冲突——它约束的是上一轮的部署任务，不是本方案。

---

## 1. 先明确：你现在已经有「音色克隆」了

当前部署的克隆是**零样本（zero-shot）**——**不需要训练**：

| | 零样本克隆（现有） | 微调（本方案） |
|---|---|---|
| 需要什么 | 一段 3~10 秒参考音频 + 它的文本 | 1~30 分钟该人声的干净语料 |
| 训练耗时 | **无** | 数十分钟~数小时 |
| 效果 | 即时可用，音色有几分像 | 更像、更稳、韵律更自然 |
| 换音色成本 | 换一段音频即可 | 每个音色都要重训 |

**建议：先用零样本试效果**（用法见 `Linly-Talker使用手册.md` §6.5）。不满意再回来做微调。

> 用法速览：
> ```bash
> -F "voice=gptsovits:<服务器上的音频路径>|<该音频的文字>|中文"
> ```
> 约束：音频 3~10 秒、必须是服务器本地路径（API 不收音频上传）、语言写中文名。

---

## 2. 为什么不能在本仓库里训练（已实跑验证）

**结论：Linly-Talker 仓库里的 GPT-SoVITS 是「推理 + 预处理脚本」的残骸，训练链路被上游摘掉了。**

这不是配置问题，是**代码缺失**。三条硬证据：

### 证据 1：`tools/` 包根本不存在

```
$ cd /root/autodl-tmp/Linly-Talker/GPT_SoVITS && ls -d tools
ls: cannot access 'tools': No such file or directory

$ python -c "from process_ckpt import savee"
ModuleNotFoundError: No module named 'tools'
```

`process_ckpt.py:5` → `from tools.i18n.i18n import I18nAuto`，而 `GPT_SoVITS/tools/` **在 git 历史里从未存在过**（`git ls-files | grep -c "GPT_SoVITS/tools"` = **0**）。

⇒ `s2_train.py` 连 import 都过不去。同样受影响的还有 `inference_webui.py:67`、`inference_gui.py:7`——**上游原生推理脚本也起不来**。

### 证据 2：配置文件全都缺训练必需的键

`configs/` 下 7 个 yaml，逐个核对：

| 键 | `s1longer.yaml` | 用途 |
|---|---|---|
| `output_dir` | ❌ 缺失 | s1_train.py:88 读取，缺则 KeyError |
| `train_semantic_path` | ❌ 缺失 | s1_train.py:133 |
| `train_phoneme_path` | ❌ 缺失 | s1_train.py:134 |
| `exp_name` | ❌ 缺失 | s1_train.py:100 |
| `if_save_latest` | ❌ 缺失 | s1_train.py:97 |
| `half_weights_save_dir` | ❌ 缺失 | s1_train.py:99 |

`configs/s2.json` 同样缺 `gpu_numbers`、`exp_dir`、`pretrained_s2G/s2D`、`save_weight_dir`。

**上游是靠 WebUI 在运行时动态生成这些配置的**，而本仓库的 `webui.py` **没有训练页签**（grep `output_dir|train_semantic_path` 在 `webui.py`/`app*.py`/`VITS/*.py` 中零命中）。

### 证据 3：数据拼装与切片/ASR 环节全缺

- `prepare_datasets/` 只有 `1-get-text.py`、`2-get-hubert-wav32k.py`、`3-get-semantic.py` 三个脚本，且**按分片输出**（`2-name2text-{i_part}.txt`、`6-name2semantic-{i_part}.tsv`）
- 但训练侧读的是**合并后的单文件**（`module/data_utils.py:32`、`AR/data/data_module.py:31`）
- **没有任何脚本负责合并**——上游是在训练 WebUI 的「1B」页签里做的
- 切片工具（`tools/slicer.py`）和批量 ASR 标注（`tools/asr/*`）**随 `tools/` 一起缺失**

### 上游作者本人的说明

`VITS/README.md:19` ——

> 「**为了尊重作者，我并没有把 `GPT-SoVITS` 的全套代码搬过来**，我写了一个关于语音克隆的类，大家可以将**训练好的模型参数**中，就可以在本项目使用经过语音克隆后的 TTS 了」

**这正是本方案的做法**：在上游完整仓库里训练，把产物拷回来用。

### 附带澄清：`checkpoints/` 里的 `May.pth` / `Obama.pth` 不是音色模型

它们是 **ER-NeRF 的数字人（口型驱动）权重**，与语音无关——体量只有 7~8 MB（真正的 SoVITS 是 ~90~101 MB，GPT 是 ~148 MB），且被 `TFG/NeRFTalk.py:133` 这样调用：`nerf.init_model('./checkpoints/Obama_ave.pth', './checkpoints/Obama.json')`。

`Obama.json`（4.4 MB）是 NeRF 相机位姿 JSON，首字节即 `{"focal_len":1200.0,"cx":256.0,...}`。

---

## 3. 方案总览

```
┌─ 本机（AutoDL，RTX 4090）────────────────────────────────┐
│                                                          │
│  ① 另拉上游 GPT-SoVITS 到独立目录（不动 Linly-Talker）    │
│  ② 建独立 conda 环境 GPTSoVits                           │
│  ③ 复用 Linly-Talker 已有的预训练权重（省 1.1 GB 下载）   │
│  ④ 准备语料：切片 → 降噪(可选) → ASR → 人工校对           │
│  ⑤ 训练 s1（GPT）+ s2（SoVITS）                          │
│  ⑥ 产物拷回 Linly-Talker                                 │
│  ⑦ 适配层加一个入口（仓库外，允许改）                     │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

**关键原则**：**在 Linly-Talker 仓库外做完一切**，仓库保持 `git status` 干净——与现有部署方案的设计一致（服务代码在 `/root/autodl-tmp/linly-api/`，不进仓库）。

---

## 4. 前置检查

### 4.1 磁盘（最容易翻车的地方）

当前 **50G 已用 27G，可用 24G**。本方案需要约 **10~13G**：

| 项 | 占用 |
|---|---|
| 上游仓库代码 | ~1 G |
| 新建 conda 环境（含 PyTorch） | ~6~8 G |
| 预训练权重 | **0**（复用已有的）或 1.1 G |
| G2PW 中文模型 | ~0.6 G |
| ASR 模型（首次使用自动下载） | ~1 G |
| 语料 + 训练中间产物 + ckpt | ~1~2 G |

**建议先腾空间**（按收益排序，均不影响现有服务）：

```bash
# ① LLM 权重，API 未接线，不用就删 —— 释放 3.5G（收益最大）
du -sh /root/autodl-tmp/Linly-Talker/Qwen

# ② conda 包缓存
conda clean -a                    # 释放约 645M

# ③ 临时残留
rm -rf /root/autodl-tmp/.tmp/*    # 释放约 238M
```

### 4.2 显存

本机 RTX 4090 24564 MiB，**足够**。上游 s1 配置 `batch_size: 8 × grad_accum 4`、s2 配置 `batch_size: 32`。显存不够时可下调 batch size（会变慢）。

### 4.3 训练期间不要同时跑服务

训练吃满显存，与 6006/6008 服务互斥。**先停服务**：

```bash
pkill -f "[u]vicorn app.main:app"
```

### 4.4 ⚖️ 授权提醒

上游作者在 `常见问题汇总.md:98-101` 说明了他们**不提供克隆语音的权重**，理由是可能涉及版权。

**只克隆你有权使用的声音**（自己的、已获授权的、或有明确许可的）。克隆他人声音用于公开发布可能涉及法律风险。

---

## 5. 分步操作

### 步骤 0：拉取上游代码（⚠️ 请你自己执行）

> 按既定约定，拉取类操作由你手动执行。

```bash
cd /root/autodl-tmp
git clone https://github.com/RVC-Boss/GPT-SoVITS.git GPT-SoVITS-upstream
cd GPT-SoVITS-upstream && git log --oneline -1
```

**为什么放在 `/root/autodl-tmp/GPT-SoVITS-upstream`**：与 Linly-Talker 平级、互不干扰；两个仓库各有一份 `GPT_SoVITS/` 目录，**放在一起极易混淆**。

若 GitHub 直连慢，用镜像：
```bash
git clone https://ghproxy.net/https://github.com/RVC-Boss/GPT-SoVITS.git GPT-SoVITS-upstream
```

### 步骤 1：建独立 conda 环境

**不要复用 `linly` 环境**——那个环境钉死了 `mmcv 2.1.0`（含 CUDA ops）、`mmpose 1.3.2` 等，与上游依赖有冲突风险。

```bash
source /root/miniconda3/etc/profile.d/conda.sh
conda create -n GPTSoVits python=3.10 -y
conda activate GPTSoVits

cd /root/autodl-tmp/GPT-SoVITS-upstream
bash install.sh --device CU126 --source HF-Mirror
```

> `--source` 可选 `HF` / `HF-Mirror` / `ModelScope`。国内建议 `HF-Mirror` 或 `ModelScope`。
> 不想用 `install.sh` 也可以手动：
> ```bash
> pip install -r extra-req.txt --no-deps
> pip install -r requirements.txt
> conda install ffmpeg -y
> ```

> 💡 **AutoDL 下载慢的真凶**：CDN 对 HTTP/1.1 限速。若 pip 卡住，用 `curl --http2` 单独取 wheel 再本地装。

### 步骤 2：预训练权重 —— 复用已有的，省 1.1 GB

上游要求把权重放到 `GPT_SoVITS/pretrained_models/`。**这些文件 Linly-Talker 里已经有一份**（`pretrained_models/README.md` 明确写了它们来自 `RVC-Boss/GPT-SoVITS`，同一来源）。

| 文件 | 大小 | 是否可复用 |
|---|---|---|
| `chinese-hubert-base/`（`pytorch_model.bin`） | 180 MB | ✅ |
| `chinese-roberta-wwm-ext-large/`（`pytorch_model.bin`） | 621 MB | ✅ |
| `s1bert25hz-2kh-longer-epoch=68e-step=50232.ckpt` | 148 MB | ✅ |
| `s2G488k.pth` | 101 MB | ✅ |
| `s2D488k.pth` | 89 MB | ✅ |

**硬链接过去（不占额外空间）：**

```bash
SRC=/root/autodl-tmp/Linly-Talker/GPT_SoVITS/pretrained_models
DST=/root/autodl-tmp/GPT-SoVITS-upstream/GPT_SoVITS/pretrained_models
mkdir -p "$DST"
cp -al "$SRC"/chinese-hubert-base "$SRC"/chinese-roberta-wwm-ext-large "$DST"/ 2>/dev/null \
  || cp -r "$SRC"/chinese-hubert-base "$SRC"/chinese-roberta-wwm-ext-large "$DST"/
cp -l "$SRC"/s1bert25hz-2kh-longer-epoch=68e-step=50232.ckpt \
      "$SRC"/s2G488k.pth "$SRC"/s2D488k.pth "$DST"/ 2>/dev/null \
  || cp "$SRC"/s1bert25hz-2kh-longer-epoch=68e-step=50232.ckpt \
        "$SRC"/s2G488k.pth "$SRC"/s2D488k.pth "$DST"/
ls -la "$DST"
```

> `cp -al` 建硬链接，跨文件系统会失败并自动回退到 `cp -r`（两者都在 `/root/autodl-tmp` 下，应当能成功）。

**仍需单独下载的**（上游 README 第 203、205 行）：

- **G2PW 中文模型** → 解压后重命名为 `G2PWModel`，放到 `GPT_SoVITS/text/`
  （仅中文 TTS 需要；HF 或 ModelScope 均可下载）
- **UVR5 人声分离权重**（仅当你要从带伴奏的音频里提取人声）→ 放到 `tools/uvr5/uvr5_weights`
- **ASR 模型** → 首次使用自动下载，无需手动

### 步骤 3：准备语料

#### 3.1 录音要求（这一步决定效果上限，比训练参数重要得多）

| 项 | 要求 |
|---|---|
| 环境 | 安静、无回声、无背景音乐/风扇声 |
| 内容 | 单人朗读，语气平稳自然 |
| **采样率** | **≥ 32 kHz**（训练用 32k，低于此会先被重采样，损失细节） |
| 格式 | wav（推荐，无损） |
| 时长 | **5~30 分钟**最佳。1 分钟就能出效果，但更多数据明显更好 |
| 覆盖 | 尽量覆盖目标场景会用到的发音：多音字、数字、中英混读 |
| 避免 | 笑声、口头语（「嗯」「啊」）、拖长音、口齿不清、语速剧变 |

> 目标是**干净、单一说话人、无重叠**。宁要 5 分钟干净语料，不要 30 分钟带噪语料。

#### 3.2 处理流程（在上游 WebUI 里做）

启动上游 WebUI：

```bash
conda activate GPTSoVits
cd /root/autodl-tmp/GPT-SoVITS-upstream
python webui.py
```

**上游 WebUI 的端口**（已核实上游 `config.py:140-145`）：主界面 **9874**，另有 9873（UVR5）/ 9872（推理）/ 9871（标注），API 9880。**与 6006 不冲突**。

> 🔴 **但 AutoDL 只映射 6006 / 6008 到公网，9874 从外面打不开。**
> 访问上游 WebUI 有两条路：
>
> **① 端口转发（推荐）**：在你自己的机器上跑
> ```bash
> ssh -L 9874:127.0.0.1:9874 -p <AutoDL实例SSH端口> root@<实例SSH地址>
> ```
> 然后本地浏览器开 `http://127.0.0.1:9874`。
>
> **② 临时占用已映射端口**：先把 6006 的服务停掉，再
> ```bash
> python webui.py --port 6006
> ```
> 用完记得把服务启回来。
>
> ⚠️ 上游 WebUI **没有鉴权**——走 ② 意味着训练界面直接暴露在公网，训练完**立刻停掉**。

进入 **`1-GPT-SoVITS-TTS`** 页签，按上游给出的顺序操作：

```
1. 填入音频路径
2. 切片（Slice the audio into small chunks）
3. 降噪（Denoise，可选）
4. ASR（自动语音识别）
5. 校对 ASR 转写文本        ← 不要跳过！
6. 切到下一个页签，开始微调
```

**第 5 步「校对」不能省**：ASR 的错字会直接教坏模型，导致合成时读错字。逐条听一遍、改一遍。

#### 3.3 数据集格式（若你想手工构造）

上游标准 `.list` 格式，四段竖线分隔：

```
vocal_path|speaker_name|language|text
```

语言码字典（⚠️ **这里是语言码，与推理侧的「中文名」写法不同**）：

| 码 | 语言 |
|---|---|
| `zh` | 中文 |
| `ja` | 日文 |
| `en` | 英文 |
| `ko` | 韩文 |
| `yue` | 粤语 |

示例：
```
/root/autodl-tmp/data/001.wav|speaker1|zh|今天天气不错。
/root/autodl-tmp/data/002.wav|speaker1|zh|我们开始训练模型。
```

> 🔴 **别混淆**：训练 list 里用 `zh`，而**推理时** `voice=gptsovits:...|...|中文` 必须写**中文名**。两者不一致，写错会直接报错。

### 步骤 4：训练

按上游 WebUI 顺序，**先 s1（GPT）后 s2（SoVITS）**。

#### 阶段一：s1 —— GPT 训练

GPT 阶段学的是「文本 → 语义 token」的映射。上游 WebUI 里填：
- 实验名（`exp_name`）
- 训练轮数（先按默认）
- 批大小

产物：`<exp_name>-e{N}.ckpt`（如 `Gnews-e15.ckpt`）

#### 阶段二：s2 —— SoVITS 训练

SoVITS 阶段学的是「语义 token → 音频」的声学映射，**音色主要来自这一步**。

产物：`<name>_e{N}_s{N}.pth`（如 `Gnews_e8_s96.pth`）

> **参数怎么调**：先用默认值跑通一轮，再针对性调。首次训练不要动学习率。
> **中途可以停**：产物按 epoch 保存，看着差不多就能停（GPT-SoVITS 通常不需要跑满）。

### 步骤 5：产物拷回 Linly-Talker

训练完成后，把两个产物按约定放好：

```bash
# 用你的模型名替换 <模型名>，如 myvoice
mkdir -p /root/autodl-tmp/Linly-Talker/GPT_SoVITS/pretrained_models/<模型名>

cp /root/autodl-tmp/GPT-SoVITS-upstream/GPT_SoVITS/SoVITS_weights/<name>_e8_s96.pth \
   /root/autodl-tmp/Linly-Talker/GPT_SoVITS/pretrained_models/<模型名>/sovits.pth

cp /root/autodl-tmp/GPT-SoVITS-upstream/GPT_SoVITS/GPT_weights/<exp_name>-e15.ckpt \
   /root/autodl-tmp/Linly-Talker/GPT_SoVITS/pretrained_models/<模型名>/gpt.ckpt
```

> 产物实际落盘位置以上游 WebUI 显示为准（通常是 `GPT_SoVITS/GPT_weights/` 与 `GPT_SoVITS/SoVITS_weights/`）。
> **重命名成固定的 `gpt.ckpt` / `sovits.pth`** 是为了让适配层能按目录名解析，不用记 epoch 号。

**把模型目录加进 `git/info/exclude`**，保持仓库干净：

```bash
echo "GPT_SoVITS/pretrained_models/<模型名>/" >> /root/autodl-tmp/Linly-Talker/.git/info/exclude
```

### 步骤 6：接进服务（改适配层，不动项目源码）

**现状**：适配层把权重路径写死，且整个 GPT-SoVITS 只缓存一个模型实例。

- `/root/autodl-tmp/linly-api/app/adapter.py:42-43` —— 写死两个底座路径
- `adapter.py:500-509` `_gpt_sovits()` —— 缓存键固定为 `"gptsovits"`

**改法**（`linly-api/` 在仓库之外，**允许修改**，不违反「禁改源码」约束）：

1. `_parse_voice()`（`adapter.py:284`）新增一个 head，**与既有 `gptsovits:` 并存**，零回归风险：

   ```
   finetuned:<模型名>|<参考音频>|<参考文本>[|<语言>]
   ```

2. `_gpt_sovits()` 改成 `_gpt_sovits(model_key)`，缓存键变为 `f"gptsovits:{model_key}"`，模型路径按
   `GPT_SoVITS/pretrained_models/<模型名>/{gpt.ckpt,sovits.pth}` 解析

3. `_synthesize` 的 gptsovits 分支把 `model_key` 透传下去

调用形态：

```bash
-F "voice=finetuned:myvoice|/root/autodl-tmp/ref.wav|参考音频的文字|中文"
```

> **为什么新开一个 head 而不是改 `gptsovits:` 的语法**：既有的 `gptsovits:<音频>|<文本>|<语言>` 已经按 `|` 分割，插入模型名会破坏现有调用方。新 head 完全隔离。

**这一步需要现写代码**（约 30 行）。等你训练出产物后告诉我，我来改并实测。

---

## 6. 预期耗时与显存（估算）

> ⚠️ **以下为量级估算，不是本机实测**——上游与 Linly-Talker 的文档里**都没有给出训练耗时/显存数字**（已逐文件确认）。首次训练请以实际输出为准。

| 阶段 | 4090 上粗略量级 | 主要变量 |
|---|---|---|
| 数据切片 + ASR 标注 | 数分钟 | 语料总时长 |
| 人工校对 | **10~40 分钟** | **最耗时、也最不能省** |
| s1（GPT）训练 | 约 10~40 分钟 | 语料量、epoch 数 |
| s2（SoVITS）训练 | 约 20~60 分钟 | 语料量、epoch 数 |
| 合计 | **约 1~2.5 小时**（含人工） | — |

**显存**：24 GB 充裕。上游默认 `s2 batch_size: 32`；若 OOM 就减半。

---

## 7. 训练完怎么验证

### 7.1 先在上游仓库里验

在**上游**的 `1C-inference` 页签里加载你的模型 + 一段参考音频，合成一句话试听。**这一步不通就不要往 Linly-Talker 拷**——能隔离问题是出在训练还是出在接入。

### 7.2 再在 Linly-Talker 里验

```bash
# 起服务
nohup /root/autodl-tmp/linly-api/run.sh > /root/autodl-tmp/logs/api.log 2>&1 &
sleep 30
curl -s localhost:6006/api/v1/health | grep pipeline_impl   # 应为 linly-talker
```

用微调模型合成一句话，与零样本版本**做 A/B 对比**：

```bash
# A：零样本（底座 + 参考音频）
-F "voice=gptsovits:/root/autodl-tmp/ref.wav|<参考文本>|中文"
# B：微调模型
-F "voice=finetuned:myvoice|/root/autodl-tmp/ref.wav|<参考文本>|中文"
```

**对比要点**：相似度、稳定性（长句是否崩）、韵律自然度、有无杂音/吞字。

### 7.3 回归验收（确认没弄坏既有功能）

```bash
/root/autodl-tmp/conda/envs/linly/bin/python /root/autodl-tmp/deploy/verify_b3.py
/root/autodl-tmp/conda/envs/linly/bin/python /root/autodl-tmp/deploy/verify_c.py
```

验收线：产出 mp4 体积 **260~370 KB**（640×1024）。

---

## 8. 排错

| 现象 | 原因 | 处理 |
|---|---|---|
| `ModuleNotFoundError: No module named 'tools'` | 你在**错误的地方**跑训练——这是 Linly-Talker 仓库的老问题 | 确认在 `GPT-SoVITS-upstream/` 下操作 |
| `KeyError: 'output_dir'` / `'gpu_numbers'` | 同上，用了本仓库的残缺 config | 用上游 WebUI，别手改 yaml |
| 训练时 OOM | batch 太大 | 减小 batch size |
| 合成出来是噪音/乱码 | 训练数据太脏，或 ASR 标注错太多 | 回到步骤 3.2 第 5 步重新校对 |
| 音色不像 | 语料太少或有混响 | 增加干净语料；或先退回零样本 |
| 合成时读错字 | ASR 转写有错字 | 重新校对标注 |
| `Can't get attribute 'HParams'` | 顶层 `utils` 模块被 MuseTalk 劫持 | 适配层已处理（`_restore_gptsovits_utils_module`）；自己写脚本要复刻 |
| pip 下载卡住 | CDN 对 HTTP/1.1 限速 | 用 `curl --http2` 取 wheel 再本地装 |
| 编译类安装极慢 | 缺 `ninja` | `pip install ninja`（否则并行编译静默失效） |

---

## 9. 回退

**本方案对现有系统是零侵入的**：

- 上游仓库在独立目录，建独立 conda 环境
- Linly-Talker 仓库**只新增**权重目录，`git status` 保持干净
- 适配层新 head 是**纯新增**，`gptsovits:` 原行为不变

**想彻底撤销**：

```bash
rm -rf /root/autodl-tmp/GPT-SoVITS-upstream
conda env remove -n GPTSoVits
rm -rf /root/autodl-tmp/Linly-Talker/GPT_SoVITS/pretrained_models/<模型名>
# 适配层的 finetuned: 分支留着无害，也可回滚
```

服务重启后即回到当前状态，零样本克隆不受任何影响。

---

## 10. 一页速查

```bash
# ── 准备 ──────────────────────────────────────────
cd /root/autodl-tmp
git clone https://github.com/RVC-Boss/GPT-SoVITS.git GPT-SoVITS-upstream

source /root/miniconda3/etc/profile.d/conda.sh
conda create -n GPTSoVits python=3.10 -y && conda activate GPTSoVits
cd /root/autodl-tmp/GPT-SoVITS-upstream
bash install.sh --device CU126 --source HF-Mirror

# 复用已有权重（省 1.1G）
cp -al /root/autodl-tmp/Linly-Talker/GPT_SoVITS/pretrained_models/{chinese-hubert-base,chinese-roberta-wwm-ext-large} \
       /root/autodl-tmp/GPT-SoVITS-upstream/GPT_SoVITS/pretrained_models/

# ── 训练（先停掉 6006 服务，释放显存）──────────────
ssh -L 9874:127.0.0.1:9874 -p <实例SSH端口> root@<实例IP>   # 在你自己的机器上
python webui.py            # → 本地浏览器开 http://127.0.0.1:9874
#   1-GPT-SoVITS-TTS：填路径 → 切片 → 降噪 → ASR → 校对 → 微调

# ── 拷回 ──────────────────────────────────────────
mkdir -p /root/autodl-tmp/Linly-Talker/GPT_SoVITS/pretrained_models/<模型名>
#   sovits.pth  ← <name>_e{N}_s{N}.pth
#   gpt.ckpt    ← <exp_name>-e{N}.ckpt

# ── 接入（待实现，约 30 行）───────────────────────
#   voice=finetuned:<模型名>|<参考音频>|<参考文本>|中文
```

**相关文档**：`Linly-Talker使用手册.md`（日常操作）、`部署进度与交接.md`（部署过程踩坑）
